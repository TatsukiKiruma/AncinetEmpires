请在当前AncinetEmpires工作区执行v7训练纠偏与迭代，不再重复四模型同一流程盲训。
先读取有效AGENTS.md、记录实际分支/HEAD/dirty状态；完整阅读AncinetEmpires_AI_Training_Advice_20260921_v7.md。审查基点为55177b8ea9459d1b30a83e44eb412fb2f112a4c4，若代码更新逐项核验，不回退已修内容。
优先执行R7-01至R7-03：移除runCoverageAudit固定数据；修正D_R30单图短轨迹与少量模板重复；取消BC/NET_A/B前N截断导致课程零消费；修C/F/G错误单位索引和非法标签自动换成第一个动作；关闭伪自然value监督；修best被last覆盖、metrics覆盖和4块TS只跑2块；恢复或明确BC特征真实身份。
先写能揭露这些实际缺陷的失败型测试，再修实际生产入口。不要直接import会触发main的旧pipeline，不运行会覆盖旧结果的入口。只做独立小检查不足以宣布完整数据/引擎通过。
然后用真实可重放、根隔离、多地图/两座位的数据训练一个Spatial-v2 32ch/2块policy-only主模型，NET_A仅作低成本对照。记录实际消费ID与比例、最佳/末轮checkpoint、Python/TS逐层和候选logit一致性。不要把生成行数当唯一状态或训练消费数。
按R7-04记录失败前关键状态并做一次教师替换的反事实回放，采集学生访问状态做一轮DAgger。Heuristic可作为追赶基线的教师，不要求它先证明能击败自己；不要把其标签视作全局最优。
按R7-05/R7-06先比较Heuristic与有界搜索增强Heuristic，再蒸馏有真实改进证据的搜索策略，并测试空间先验S10。当前伪value不得用作搜索叶值。每个底层Action端到端目标约1000ms，节点/墙钟分别记录，必须看到实际对手回应。
R7-07的数据30k→100k/150k和2块→4块有条件执行，不同时再训练四路庞大网格；4块先保证跨语言一致性。需要的真实资源缺失应指出具体BLOCKED，并继续独立任务，不填常数、不虚构PASS、不只再写计划。
R7-08统一评测：自然胜/败/平、截断、错误分开；0次重招募机会的成功率为null；每次引擎step检查执行结果，保存可回放初态、动作、RNG、失败状态和模型/配置hash。已经用过的五张图是开发基准，不宣称未见最终集。
保持游戏规则、默认Heuristic、历史模型与用户修改；不删除数据、不自动push、不调用收费云资源。新runId写新结果但不借改名绕过有效资源上限。完成实际代码、测试、训练/对战或具体资源阻塞说明，交付真实结果与NEXT_ACTION，禁止固定“全部完成”。
