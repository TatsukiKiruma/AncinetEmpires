# 服务器交接：训练集导出

## 2026-09-18 本地后续任务更新

兼容性核对及小规模试训已完成。当前本地生成器指纹与最终数据清单一致；lint、429 项测试、build、8 项 APK 报告和 forced 回放 112/112 全通过。5,000 条训练样本 + 1,000 条验证样本、1 epoch 试训及两局模型加载对战成功，无非法动作、超时或步数保护停止。试训模型两局均负，仅用于流程验证。报告见 `docs/training/PREFLIGHT_20260918.md`，产物在 `training_runs/preflight_20260918/`。正式全量模型尚未训练，云端服务仍无需恢复。

## 2026-09-18 最终完成状态（优先于下文历史记录）

- 云端在 2026-09-17 23:54 完成 **250/250 批、2,500 局**。09-18 00:32 已完成剩余数据下载、本机合并、全量校验与云端已验证分片清理。
- 完整本机数据：`C:\code\AncinetEmpires\training_runs\server_archive_20260913\dataset\`；训练入口清单为其中的 **`manifest.json`**，不要使用单批清单或历史传输 inventory 代替。
- 累计 **250 份 checkpoint manifest、406 个特征分片（共 656 文件，不含最终 manifest）**全部通过大小及 SHA256 校验；总计 25,812,272,838 字节。
- 使用下载自云端且与 `deployment_manifest.json` 哈希一致的 `skirmish_dataset_validate.ts` 执行完整校验：**valid=true、977,737 样本、886,494 训练样本、91,243 验证样本、episodeLeakageCount=0、errors=[]**。最终清单精确覆盖累计归档分片，样本总数与最终 summary 一致。
- 最终传输及验证记录：`training_runs/server_archive_20260913/final_20260918/`；包含 `inventory.json`、`completed.tar.xz`（168,940,080 字节）、`final-info.json`、匹配版本校验器、`merged-receipt.json`、`previous-local-receipt.json`、`validation-result.json`、`cleanup-result.json`。
- 根目录 `local-receipt.json` 已合并为全部 250 批；根目录 `inventory.json` 仍是首次 53 批传输记录。前两次归档及其压缩包均保留。
- 本次只删除最后 105 批中的 **171 个已验证 .jsonl 分片，3,570,349,101 字节（约 3.33 GiB）**。清理后确认云端完整 checkpoint 分片为 0、checkpoint manifest 为 250、原始 episode 文件为 19；最终 manifest、原始对局、未完成/中断现场、源码、回执及其他服务数据均保留。磁盘可用约 **18.88 GiB**。
- 云端 `runtime/offload/final_20260918/` 保留累计回执、全量验证报告及逐文件清理记录。服务保持 `inactive/dead`、`Result=success`，**不要再重启导出任务**。历史 `runtime/status.json` 仍为 `awaiting-local-validation`，它表示导出结束时状态；本机全量校验已在上述报告中通过，没有人为改写历史状态或创建 `complete.marker`。
- 下一阶段是核对本地训练代码与数据生成版本的兼容性，训练模型并评估；当前仅训练数据集完成，尚未训练新模型。
- 本次操作脚本：`tools/server_archive_final.py`，使用独立的 `final_20260918` 目录并复用增量下载逻辑；属于本次操作记录，不要直接重复执行下载或清理。

以下为历史归档及导出记录。

## 2026-09-16 增量归档更新

- 本次在导出运行时固定并下载 92 个新增完整批次；下载、解包及本机校验完成后才暂停服务清理。
- 本机累计归档 **145 批**，共 **380 个文件**全部重新通过大小和 SHA256 校验；数据仍在 `training_runs/server_archive_20260913/dataset/checkpoints/`。
- 本次独立记录位于 `training_runs/server_archive_20260913/incremental_20260916/`：`inventory.json`、`completed.tar.xz`、`merged-receipt.json`、`previous-local-receipt.json`。根目录 `local-receipt.json` 已更新为累计回执；根目录 `inventory.json` 仍是首次 53 批传输清单。
- 云端累计回执已合并为 145 批，实际调用已部署的 `archivedCheckpointValid` 对全部累计清单验证通过。仅删除新增清单列出的 **147 个 .jsonl 特征分片，12,188,146,348 字节（约 11.35 GiB）**；未删除任何 manifest、原始对局、未完成批次或其他任务数据。
- 云端操作记录：`runtime/offload/incremental_20260916/` 下的 `inventory.json`、`previous-receipt.json`、`merged-receipt.json`、`cleanup-plan.json`、`cleanup-result.json`。压缩流直接下载，本次没有在云端生成压缩包。
- 暂停前已完成 **146/250 批（58.4%）**，第 146 批是在下载期间完成的，仍完整保留在云端。13:45:44 已恢复服务，特征并发保持 2；清理后磁盘剩余约 20 GiB。执行下一次操作前重新查实时状态。
- 13:50 已确认续跑日志“已校验并跳过 146 个完整 checkpoint”，两个 worker 的临时分片均重新写入，恢复成功。本次压缩包为 506,364,336 字节（约 483 MiB），累计本地数据为 22,239,775,952 字节；清理后实测可用空间 20.07 GiB。
- 本次脚本 `tools/server_archive_incremental.py` 固定本次传输目录，不能直接复用于下一次归档；下一次应使用新的独立传输目录并继续合并累计回执。未修改部署源码、生成器指纹或训练参数。

以下为较早快照，归档数量以上述更新为准。

更新：2026-09-14 09:26，北京时间。本文是操作入口；执行前重新查询实时状态，不把下列快照当作当前值。

## 任务与当前状态

- 用户目标：生成全部训练集。目前只是对局生成、特征导出和数据校验，不是训练模型。
- 服务器 `111.230.73.106`，Ubuntu，4 逻辑核、约 3.64 GiB 内存、约 40 GiB 系统盘；同机运行 AutoBand Docker 服务，不能误停或清理它们。
- 2,500/2,500 局已生成；特征导出 **68/250 批（27.2%，680 局）**，每批 10 局。其中 **53 批在本机归档，15 批在云端新增完成**，不要重复加总。
- 服务运行中，特征 **2 worker**，正在处理 `sd-low-unit-limit-episodes-e00080-00089` 和 `e00090-00099`。最近完成批次时间 08:45，两个临时分片在 09:26 更新。
- 实测任务 CPU 占整机 50.8%，任务内存约 1.73 GiB，当前无明显内存压力、无 OOM；磁盘已用约 19.84 GiB，剩余 **17.71 GiB**。
- 之前 3 个特征 worker 出现严重内存限流，用户指定降至 2；不要自行调回 3。历史对局生成并发为 3，但该阶段已经完成。

## 连接与权限

本机工程：`C:\code\AncinetEmpires`。PowerShell 连接方式：

```powershell
$serverSsh = @('-i', 'C:\Users\SHIINA\Downloads\chat.pem', '-o', 'IdentitiesOnly=yes', '-o', 'BatchMode=yes', '-o', 'StrictHostKeyChecking=yes', '-o', 'UserKnownHostsFile=C:\Users\SHIINA\AppData\Local\Temp\autoband-deploy-known-hosts', 'ubuntu@111.230.73.106')
& ssh @serverSsh 'systemctl status ancientempires-training.service --no-pager'
```

密钥仅引用路径，不输出内容。若 known_hosts 文件缺失或主机密钥不匹配，核实可信指纹，不关闭主机校验。Codex 环境的网络/SSH 命令通常需要 `require_escalated`，按工具审批机制执行。

用户此前已授权上传代码与数据、运行全量任务、调整特征为 2 worker，以及下载并校验完成批次后清理对应云端分片再续跑；仍以新对话的具体指令为准，不扩展到其他服务或数据。

## 目录与查询控制

以下为远端 shell 命令；也可作为字符串交给上面的 SSH：

```sh
cd /home/ubuntu/ancientempires-training-20260909
cat runtime/status.json
cat training_runs/feature_datasets/*/*/migration-status.json
tail -n 25 runtime/features.log
df -h .
free -h
systemctl show ancientempires-training.service -p ActiveState -p SubState -p MemoryCurrent -p Result
sudo -n journalctl -u ancientempires-training.service -n 30 --no-pager
# 按用户指令暂停或恢复；先确认状态，避免重复启动。
sudo -n systemctl stop ancientempires-training.service
sudo -n systemctl start ancientempires-training.service
```

- 根目录：`/home/ubuntu/ancientempires-training-20260909`，下文路径除注明本机外均相对此目录。
- 服务：`ancientempires-training.service`，入口 `tools/server_training_pipeline.py`；已启用开机启动，`Restart=no`。
- 原始对局：`training_runs/episodes/sd_training_plan_20260705_heuristic-apk-like-balanced/`。
- 数据集目录：`training_runs/feature_datasets/sd_training_plan_20260705_heuristic-apk-like-balanced/sd_training_plan_20260705_heuristic-apk-like-balanced-v3-025ea72a667ff9b1/`。
- 数据集 ID：`025ea72a667ff9b130db436b2d6c12beb02d7ef3eccf9081378fe6f2da43f0f8`。
- 进度看数据集内 `migration-status.json`；`runtime/status.json` 给出阶段和暂停/错误原因。批次完成才更新计数，计数暂时不变时检查 `.partial` 修改时间及 CPU，不能直接判定卡死。
- cgroup：`/sys/fs/cgroup/system.slice/ancientempires-training.service/`。读 `cpu.stat` 两次取差值，除采样时长和 4 核得到整机占比；`memory.current` 包含缓存，结合 `memory.pressure` 和 `memory.events` 判断压力。
- 限制：CPUQuota=300%，Nice=10，MemoryHigh=2200M，MemoryMax=2700M，MemorySwapMax=128M。磁盘低于 5 GiB 时流水线自动暂停并保留断点。

## 已归档数据与续跑规则（必须了解）

2026-09-13 已将 53 批（530 局）下载到本机，141 个文件逐一通过字节数及 SHA256 校验：

- 数据：`C:\code\AncinetEmpires\training_runs\server_archive_20260913\dataset\checkpoints\`，约 **9.36 GiB**。
- 清单与回执：同归档根目录的 `inventory.json`、`local-receipt.json`；压缩包 `completed.tar.xz` 约 315 MiB。
- 云端保留 53 份 checkpoint manifest 和数据集内 `offloaded-checkpoints.json`；已删除对应 88 个特征分片。**这些缺失是已归档状态，不是数据损坏；不能重新计算或删除本地副本。**
- 云端正式导出改用 **`tools/server_offloaded_migrate.ts`**，验证归档回执及 manifest 哈希后跳过这些批次，新批次仍完整校验。启动日志应有“已校验并跳过 53 个完整 checkpoint”。
- 原始 `tools/skirmish_old_dataset_migrate.ts` 未修改，它参与生成器指纹。不要切回原入口或覆盖远端源码；否则可能校验失败、改变数据集 ID 或重复生成。
- `deployment_manifest.json` 检查源码哈希。合法修改远端入口时要先备份并同步对应哈希，不能笼统绕过检查。远端部署与本地开发代码可能不同，以远端实际文件为准；已部署入口和流水线副本保存在本机归档根目录。
- 重启会重新检查全部对局并扫描配额，需等待几分钟。已完成批次跳过，未完成批次移入 `interrupted/` 留存后重算；不要无故反复重启。

再次释放空间：暂停 → 仅选择云端新增完整批次 → 压缩下载 → 本地解包并核对每个文件大小/SHA256 → **将新回执与已有 53 批记录合并** → 确认续跑识别合并记录 → 仅删除已验证分片 → 恢复 2 worker。保持所有 manifest、原始对局和回执。Windows 使用短目录 `dataset`，原长数据集目录名曾触发路径长度限制；低带宽优先 XZ 压缩。

本机 `tools/server_archive_prepare.py`、`server_archive_download.py`、`server_archive_install.py` 是首次归档操作记录，**不能原样重跑第二次**：准备脚本会遇到已归档缺失分片，安装脚本会覆盖旧回执，下载脚本会覆盖本次传输记录。再次归档必须改成增量选择、独立传输记录和累计回执，并核对已有归档。

## 全部完成后的收尾

正式参数：19 个方案、每局最多 800 样本、hashed-action-v3、4096 维、64 候选、fast-rollout（random 与 apk-like 教师）、全局 move=400000/wait=200000 配额、验证比例 0.1。不要为了续跑改参数、输入顺序或生成器指纹；完整命令见实时 `runtime/status.json.command`。

启用归档后，全部批次导出完成会进入 `paused / awaiting-local-validation`，输出最终 manifest，但不写 `complete.marker`。将剩余分片和最终 `manifest.json` 下载到本机 `dataset`，保持 `checkpoints/...` 相对路径，使用匹配版本的 `tools/skirmish_dataset_validate.ts --manifest <本机最终清单>` 做全量哈希、样本和训练/验证集隔离检查。通过后才能称为全量可用训练集。当前没有定时监控或自动下载任务。

补充记录：`training_runs/server_run_20260909/RUNNING.md` 与 `training_runs/server_archive_20260913/README.md`。
