# AncientEmpires：AI 升级完整任务书

## 正确性修复、网络深度对照与 1 秒在线决策（Agent 执行版）

**版本：** 2026-09-20 / v3.0 整合版  
**仓库：** `TatsukiKiruma/AncinetEmpires`  
**前序源码审计分支：** `qwen`  
**前序源码审计提交：** `cfb07fcc97aff5c2155d3289ddb834ffd1f7522d`  
**该提交时间：** 2026-09-19 20:53:37 UTC / 2026-09-20 05:53:37 Asia/Tokyo  
**必读配套文件：** `AncinetEmpires_AI_Progress_Review_20260920.md`（v3.0）  
**历史参考：** `AncinetEmpires_AI_Agent_Taskbook_20260919.md`（若存在，仅作历史；不是执行本书的必要依赖）  
**文档性质：** 将前序审计任务与用户新增的“可适度加深网络、决策时间可放宽到 1 秒”要求合并的完整执行规范。本轮编写读取了前版两份文档并复核方法/运行时官方资料，没有再次审计移动中的远端分支，也没有执行项目测试、训练或对战。事实部分只针对上述固定提交；运行 Agent 必须重新核对当前工作区。

> 核心决策：先保证输入、数据、搜索和加载正确；保留已有实现，不推倒重来。随后对比 NET_A 两层干路与 NET_B 三层干路，在线统一按每个底层动作端到端 1,000 ms 验收。优先验证“适度网络＋有效对手回应搜索”，不预设更深网络或价值头必然更强。

## 0. 执行 Agent 首先读这一节

本任务书是可以独立执行的整合计划，不是要求从 T00 重写到 T14。所需约束、任务、预算、验收和交接要求均已在本书重述；只放置本书与配套审查报告，也足够启动。若本机已经存在更新实现或实验，先核验并复用，不为满足文件名而重复实现。

先完成 **N00—N04 的必要正确性工作，并并行准备 N05 的冻结协议**，交出 `Gate A2`。Gate 通过、依赖具备且总预算允许时，继续执行后续任务，不应只交一份计划便结束。教师未取得资格时，仍可用可信已有行为数据完成修复后的 A/B 基线实验；只阻塞依赖该教师的纠错采集与“强教师”标签生产。证据不足登记 `NOT_EVIDENCED/HOLD`，真实缺资源或依赖登记 `BLOCKED`，继续其他可独立任务。

本轮首先要回答三个问题：

1. 同一局面下，真实后果不同的重要合法动作，是否会被编码成同一个向量？
2. 搜索最后真正展开了哪些动作，是否真的看到了对手的有效回应？
3. 在相同评估与预算下，教师、学生和网络辅助搜索各自有没有收益？

**不要以“已写到 T11”“合成数据过拟合成功”“训练准确率更高”作为棋力验收。**

### 0.1 两份根目录文档的职责与冲突处理

```text
<项目根目录>/
  AncinetEmpires_AI_Agent_Taskbook_20260920.md    ← v3.0，执行规范
  AncinetEmpires_AI_Progress_Review_20260920.md   ← v3.0，固定提交审计与决策背景
  AncinetEmpires_AI_Agent_Taskbook_20260919.md   ← 可选，历史任务书
  package.json
  src/
  tools/
```

先通读两份 v3.0 文件，分页读取到文件末尾；不能只读取前几十行或摘要。旧任务书存在时，只补读相关任务背景，不再从 T00 重做。权限、安全与用户最新明确要求始终优先；本轮工作目标以本书为准，审查报告不独立下达另一套任务。源码和可复验产物决定事实，不能为符合报告而否认已经修好的代码。发现版本冲突，写入 N00 差异记录，保留旧文件，不自行回退分支。

### 0.2 已确定要求、工作约定与待测建议

| 类型 | 内容 | 执行含义 |
|---|---|---|
| 用户已确定 | 在线决策时间可以放宽到 1 秒 | 不再以极短单次推理时间为唯一目标；上限不是强制等待时间 |
| 本书工作约定 | 每个底层动作、调用方端到端 1,000 ms | 包含排队、状态传递、编码、推理、搜索、返回和合法性校验；整回合另报 |
| 待测架构建议 | NET_A `[256,128]` 对 NET_B `[256,256,128]` | 是本轮主要 A/B 候选，不是现有 checkpoint 的真实宽度，也不是承诺 B 更强 |
| 待测时间分配 | 总耗时约 850 ms 停止新增搜索；约 950 ms 返回当前合法结果 | 从同一个 t0 起算，给 1,000 ms 留余量；允许先经性能预检调小并冻结 |
| 条件任务 | NET_C、参数量匹配加宽、精确网格/残差网络、第二轮纠错 | 仅在前置结果与剩余预算支持时执行；不自动扩成参数网格搜索 |
| 未获授权 | 收费云算力、外部上传、默认 AI 切换、远端发布 | 本书和启动 prompt 不赋予这些权限 |

一次决策 = 返回一个引擎底层 Action，不是整局、整个玩家回合、一个候选评分或一层搜索。网络深度也不是搜索深度。当前参考网络沿状态到策略输出已有 4 个隐藏层（共享干路 2 层＋策略头 2 层）；所谓“三层版”在本书中特指共享干路 3 层。[S10]

### 0.3 执行边界与工作区保护

1. 先遵循仓库现有 `AGENTS.md` 等有效工程约定，再改代码。读取真实分支、提交和 dirty diff；不执行 reset --hard、强制切换分支、覆盖用户改动、批量删模型或历史数据。
2. 同一个修复先写可重现反例，再做最小修改，最后回归。已修好的问题标 `ALREADY_FIXED` 并引用证据，不为制造工作量反复重写。
3. 不改变游戏胜负、合法动作和规则以提升 AI 指标；不丢弃失败、后手、难地图、超时记录。不把预算耗尽当规则平局，也不把材料优势判定为自然胜。
4. 不把结果、教师分数、标签位置、seed、文件名、split、模型身份等作为预测特征；规则允许的局面信息与来源元数据分离。
5. 所有新产物带 runId、代码与 dirty-diff 指纹、规则/表示/数据/模型/配置 hash。保留旧模型、旧结果、旧语义兼容入口。缺资源不冒充完成；测试未跑写 NOT_RUN。
6. 不读取或输出无关凭据，不把服务器交接文档当远端访问授权；不收费调用、不上传训练数据、不自动推送 GitHub、不改默认 AI。依赖按现有锁文件准备，新增依赖最小化并单独锁定，禁止全面升级。
7. 子 Agent 仅在实际具备能力时使用。共享一个预算账本；先分配文件所有权，公共 schema 由主 Agent 协调。不能多个 Agent 各启动 8 个训练/对局 worker。
8. 不凭文件名猜已存在 CLI。先检查参数解析与 `--help`；无入口才新增最小 CLI 和 smoke 测试。示例 schema/新参数在实现前均不是可直接执行的命令。
9. 先形成真实代码和测试结果，而非只修改计划与状态表。某任务阻塞，记录原因并继续独立任务。预算耗尽或当前执行窗口结束时保存 checkpoint/交接，不留下无授权的无限后台作业。
10. 两方 SD 是本轮主范围。三方、四方、联盟扩展、从零 PPO 和巨大 Transformer 暂缓；联盟编号与玩家编号不同的单测仍是两方正确性的一部分。

### 0.4 任务完成的判定

完成模块 ≠ 测试通过 ≠ 实战晋级。最终允许以下结果：选出更强候选；在质量非劣前提下降低成本；只取得某任务域的局部改进；或严谨证明现有候选没有收益并保留旧冠军。不得以“必须证明更深网络有效”作为验收前提。

**执行顺序摘要：** N00 盘点 → N01/N02/N03/N04 与 N05 → Gate A2 → 教师资格和可信数据上的 A/B → 有条件的单轮纠错 → 策略/价值搜索消融 → 冻结评估与可选交付。实验前冻结参数、总预算和晋级口径，不能看完结果再改阈值。


### 0.5 阅读导航（不能代替通读）

| 内容 | 位置 |
|---|---|
| 根目录文件优先级、用户要求与执行边界 | 第 0 节 |
| 固定提交进度、历史成绩与源码问题 | 第 1—2 节；配套审查报告 |
| 阶段 Gate 与任务依赖 | 第 3 节 |
| 数据/表示/搜索/数值修复 | N00—N04 |
| 1 秒请求边界、worker 与超时/陈旧响应 | N03-A/B；N05-A/B |
| 任意深度的前后向、加载与 checkpoint | N04-A |
| 教师、纠错、A/B、价值与搜索消融 | N06—N09 |
| 全轮资源账本、可用命令、交付格式 | 第 4—6 节 |
| 可复制启动 prompt；源码与方法依据 | 第 7—8 节 |

## 1. 当前状态与证据边界

### 1.1 前序固定提交审计可以确认的工程进度

| 原任务 | 本次可见实现 | 本次能给出的状态 |
|---|---|---|
| T00 / T03 | 原任务书规定了盘点、冻结评估与因子对照；旧评估工具和状态文档存在 | 新一轮完整盘点、冻结清单和验收结果需要本机证据核验 |
| T01 / T02 | 共享动作语义模块、v5 类型支持、候选顺序相关测试文件已经存在 | 实现有进展；不能把旧语义修复自动视为所有新编码器均正确 |
| T04 | `skirmish_dataset_vnext.ts`，行为/教师/结果分轨及迁移校验 | 已实现基础契约，但发现固定动作索引与完整性校验问题 |
| T05 | `src/game/ai/planning.ts`，真实引擎克隆、根阵营视角、规则终局与预算分离 | 代码已实现；性能数据和本次复跑结果未取得 |
| T06 | `skirmish_mlp_train.ts` 及测试、既有线性 BC 工具 | 实现存在；不据文件名推定新训练已完成或棋力已提升 |
| T07 | 浅层 beam/rollout 搜索教师，增加预筛选 | 已实现；不是完整 MCTS，也不是有证明的 minimax |
| T08 | 搜索代码与提交信息引用教师阶段 | 本次未取得新教师资格报告、配对对战原始结果，记 `NOT_EVIDENCED` |
| T09 | DAgger 采集记录到 vNext 的映射模块 | 契约适配已实现；采集数量、轮次质量和重训收益需本机核验 |
| T10 | 356 维状态、32 维动作编码，纯 TypeScript 双头网络、手写训练与保存加载 | 原型已实现；合成过拟合测试存在；真实新网络成绩未取得 |
| T11 | 网络动作评分器、价值叶评估器、纯策略工厂，最新提交透传叶评估器 | 接线已实现；同预算棋力或成本收益未取得 |
| T12—T14 | 原计划保留 | 不因实现 T11 自动启动无限对战、多人扩展或默认 AI 切换 |

证据见 [S01]—[S12]、[S15]—[S18]。上述“未取得”描述本次远端审计的证据范围，不等于断言开发者本机没有运行。

### 1.2 最近能够引用的历史实战结果

`docs/training/CURRENT_TRAINING_STATUS.md` 的最新实质记录仍为 **2026-09-18**。同一历史独立种子矩阵记录如下。[S02]

| 策略 | 正常开局自然胜 | 优势残局自然胜 | 全矩阵超时 |
|---|---:|---:|---:|
| 原 v4 + 启发式收官辅助 | 8/16，50.0% | 11/12，91.7% | 1/28 |
| 原纯 v4 | 2/16，12.5% | 8/12，66.7% | 4/28 |

这说明历史组合策略优于同矩阵中的纯 v4；**不能据此证明 BC 权重是主要增益来源，更不能把这些成绩移植给新双头网络或新搜索教师。** 下一轮必须加入“无 BC、但保留完全相同辅助”的对照。

同份报告还记载：2,500 局云端归档、977,737 条样本已完成历史校验；某次按验证集选中的 25% 混合补训权重在实战中退步，保留旧权重；Icy Paths 正常开局 0/4。文档更下方的 698/2,500 和 27.9% 是更早历史快照，不是当前训练完成率。[S02]

仓库 `.gitignore` 排除了 `training_runs/` 和日志。新模型、分区清单与运行证据可能只保存在本机。本轮先补可追溯的小型报告，而不是把大型数据强行纳入 Git。[S03]

### 1.3 状态词汇必须统一

每个任务分别记录 `implementationStatus`、`testStatus`、`qualityStatus`，禁止一个 `DONE` 包办三种含义。

- `IMPLEMENTED`：源码存在且已审查到对应实现。
- `TEST_PRESENT`：测试代码存在，但不代表本次已经运行通过。
- `REPRODUCED`：在指定提交、环境、输入和命令下取得可追踪的成功结果。
- `QUALITY_ACCEPTED`：按预先冻结的质量与成本标准验收通过。
- `NOT_EVIDENCED`：本次没有足够执行证据；不可补写虚构数字。
- `HOLD`：已有结果，但精度、覆盖或收益不足以作晋级决策。
- `BLOCKED`：有明确缺失资源或依赖，必须记录原因与可继续任务。

前序审计及本次文档整合不对新训练的 Gate B / C 发放 GO。对新增路径执行 Gate A2 是重新验证受影响链路，并不等于判定所有历史工作无效。

## 2. 必须优先处理的源码发现

下列“源码确认”表示可以直接从函数控制流或字段赋值确认；**尚未在本轮执行最小反例，也未测出实际数据的影响比例。** Agent 必须先补反例，再统计真实影响，不得把潜在影响写成已发生的训练损失。

### F01｜新动作编码再次遗漏真实目标和招募位置【P0，源码确认】

文件：`tools/skirmish_network_features.ts::encodeGameAction`。[S05]

几何段只有在 `actor && dest` 时写入。两类招募动作没有 `actor`，因此即便取得了城堡或部署位置，坐标也不会写入。相同兵种在不同城堡、不同部署格的动作，可以得到相同向量。`destroy_town` 又退回行动者位置，没有读取 `action.target`。攻击、治疗、支援的目标只编码少量数值，没有目标坐标，数值相同的不同目标也存在碰撞。

同一状态下若 `phi(a)=phi(b)`，当前共享状态嵌入的网络必然输出相同 logit；增加训练数据和网络宽度无法解除这一确定性不可区分性。最新提交修复的是 move 落点的一类碰撞，并未覆盖上述动作。

**修复方向：** 复用已经存在的 `getActionSemantics()`，明确编码 actor、target、landing、sourceCastle、deploy；给新表示独立版本，不改写旧模型语义。[S06]

### F02｜迁移时把候选数组位置写成固定动作索引【P0，源码确认】

文件：`tools/skirmish_dataset_vnext.ts::migrateLegacyFeatureSample`。[S07]

重标注分支使用：

```ts
fixedActionIndex: relabeled
  ? candidates.findIndex(candidate => candidate.actionCode === behaviorActionCode)
  : Number(idx)
```

`findIndex` 是抽样候选数组的局部位置，不是全局固定动作空间索引。原特征候选接口没有可靠的逐候选全局索引字段，因此不能靠更换一个属性名修复。[S17]

**修复方向：** 从可重建状态与正式动作编码器计算行为动作的固定索引；重建不了时保留显式 unknown/隔离状态，或增加清晰的可空索引契约。不得随手填 0、数组位置或标签动作索引。影响范围要按旧迁移产物是否实际进入训练分层说明。

### F03｜“多来源保留”被最终启发式排序截断抵消【P0，源码确认】

文件：`tools/skirmish_search_teacher.ts` 的根候选流水线。[S09]

```text
全合法动作 → prefilter → 多来源 pool
          → 按 heuristicScore 排序 → slice(0, beamWidth) → 展开
```

模型高分、capture 或探索项被加入 pool，不保证最终进入 beam。预筛选之外的网络高分动作也无法被 pool 再救回，因为 pool 只遍历预筛选集合。当前 win-now 判断发生在最终 beam 的动作施加之后，不能宣称覆盖全部合法立即获胜动作。

**修复方向：** 把保护/来源配额约束落实到最终展开集合；记录每层存活数、淘汰原因和溢出策略；补“启发式低分但网络强烈推荐/确实立即获胜”的反例。不要求盲目穷举所有动作，但必须准确说明搜索的保留保证。

### F04｜未真正交给对手时，可能把己方动作当对手回应【P0，源码确认条件分支】

文件：同一搜索模块的 `handToOpponent`、`evalOpponentReply`。[S09]

`handToOpponent` 在仍是己方、但没有合法 `end_turn` 时可能返回 `stop: 'ok'`。调用方随后没有先确认阵营已切换，就进入“对手回应”。涉及 pending/堆叠招募等局面时，应由规则层构造可达反例验证。

此外，`sawOpponentPhase` 是整个决策的共享标记，某个候选看过对手不等于最终选中候选看过。默认 `opponentPhaseActions=1` 只看一个底层动作，遇到“先移动再攻击”不能声称已覆盖完整战术回应。

**修复方向：** 显式交接状态、逐候选回应证明、selected-candidate 完整性、有效回应深度测试；预算不足就降级，不通过修改游戏规则跳过 pending。

### F05｜紧凑状态表示与注释存在落差【P1，源码确认结构，棋力影响待测】

4×4 分箱是有损空间聚合，不是保留完整地图的 padding。实体仅取己敌各 10 个，状态类别被压成有无，排序最后使用单位 ID。相同格、相同排序前缀但不同实体语义时，重命名 ID 可能改变实体顺序。不能据此直接宣判该模型一定弱，但需要表示碰撞、ID 重命名和不同规模局面的覆盖审计。[S04]

优先补 actor/target 的精确局部信息及可达性特征；仅在数据表明空间聚合是瓶颈时升级完整网格/实体网络。不要因为旧任务书建议过残差网络就机械重做架构。

### F06｜损失口径、加载容错与推理分配需要补契约【P1，源码确认】

双头网络的价值梯度随总样本数 N 平均，而报告的 value loss 随有效价值样本数 M 平均。这可以是一种有意设计，但报告与实际目标不一致时，M/N 变化会改变价值头相对权重，必须明确公式和覆盖率，不能简单指认反向传播错误。[S11]

模型加载只检查 kind 后把权重赋给随机初始化网络；缺失权重可能保留随机值。标准化折叠未严格验证 sigma，也未阻止重复折叠。推理调用 `toDense()` 仍会分配训练用梯度数组。网络叶评估器把非有限输出变为 0，会遮蔽上层本应触发的回退。[S10]—[S12]

### F07｜数据“有哈希”不等于完整性与结果已验证【P1，源码确认】

vNext 校验重算 `sampleIdSeed` 的哈希，但没有全面验证种子字段与实际 behavior/provenance 等字段的一致性；也不能证明快照文件存在或终局已重放。DAgger 映射把联盟编号赋给名为 `perspectivePlayerId` 的字段，需拆分玩家/联盟语义。当前映射层对训练分区进行了检查，这是应保留的正确方向；通用训练加载入口仍应独立拒绝非训练分区和错误结果契约。[S07][S08]

### F08｜“轻量化”应由剖析证明，而不是由名称证明【P1】

动作编码会为每个候选再次扫描目标、单位和地图；预筛选虽然不用寻路，仍有目标遍历等成本，不是严格 O(1)。预筛选先保留全部高优先级招募，之后再限制招募代表为 2 条，并不会移除已经加入的招募；所以其招募限流描述需要修正。width 在保护动作较多时是软上限。[S05][S09]

预算计时也有两套来源：搜索外层可以注入 `now`，规划预算内部使用 `Date.now()`。须统一可测试计时并记录昂贵评分阶段的预算超出；不要声称一次不可中断的大评分调用必然满足硬时限。[S09][S16]

## 3. 推荐路线与阶段门槛

```text
N00 本机证据盘点 / 状态修复
 ├─ N01 数据索引、分区、快照与结果契约 ─┐
 ├─ N02 网络动作语义与表示版本 ────────┤
 ├─ N03 搜索候选 / 回合 / 预算契约 ────┼─ Gate A2
 └─ N04 数值、模型加载和推理一致性 ────┘
N05 冻结评估、因子对照与性能协议 ────────┘
                      ↓
N06 教师分域资格筛选 ── Gate B2
                      ↓
N07 小规模学生状态采集 / 纠错聚合
                      ↓
N08 NET_A/NET_B 深度对照、轻量基线及有条件价值头实验
                      ↓
N09 搜索先验×价值 2×2 消融 / 质量—成本验收 ── Gate C2
                      ↓
N10 可选候选交付 / 冻结测试 / 有界下一轮 ── Gate D2
```

**Gate A2：** 受影响语义、固定索引、分区、结果、候选保留、阵营交接、模型加载、任意层数前后向与数值测试通过；在线预算骨架、合法回退、陈旧响应丢弃和可复现评估入口具备；没有未解释的关键错误。硬件完整性能/棋力验收在后续 Gate C2/D2 完成。Gate A2 前允许合成/规则夹具和受限可学性测试，不允许大规模生产强教师标签。

**Gate B2：** 教师在明确任务域、明确配置和对手下取得合格证据。`GO(endgame)` 不能自动升级为 `GO(normal/water/all maps)`。参数、粗筛或叶评价变化后，资格与旧版本绑定，不自动继承。

**Gate C2：** 在同平台、同对手和预先冻结协议下，一个学习候选至少证明质量改善，或在质量非劣前提下成本降低；在线主面板端到端每动作不超过 1,000 ms。深度结论必须来自 A/B 实验，不能从网络层数猜测；NET_A 胜出同样可以通过 Gate。仅有离线准确率上升不通过。

**Gate D2：** 冻结测试、运行时保护、可选择加载与回退齐备。默认 AI 切换仍须满足用户授权。若轻量基线已经满足目标，可以直接交付，不强制上价值头、自我对战或多人。

### 优先级与任务负责人

| ID | 优先级 | 主要产出 | 依赖 | 建议负责人 |
|---|---|---|---|---|
| N00 | P0 | 真实证据索引、当前状态、预算 | 无 | 主 Agent |
| N01 | P0 | 可重建动作索引与严格数据入口 | N00 | 数据 Agent |
| N02 | P0 | 版本化无关键歧义动作编码 | N00 | 表示 Agent |
| N03 | P0 | 可解释的最终候选与对手阶段 | N00 | 搜索 Agent |
| N04 | P1；关键失败阻塞训练 | 数值检查、严格模型加载、部署一致性 | N00；表示定版接 N02 | 模型 Agent |
| N05 | P0；可并行 | 冻结评估、消融与性能协议 | N00 | 评估 Agent |
| N06 | P1 | 分任务域教师资格 | Gate A2、N05 | 评估/搜索 Agent |
| N07 | P1 | 有限纠错数据及完整来源链 | N01、N06 | 数据 Agent |
| N08 | P1 | NET_A/NET_B 深度对照与独立开发对战 | Gate A2、N05；可信已有数据或 N07 | 模型 Agent |
| N09 | P1 | 网络×搜索增益及成本归因 | N03、N05、N08 | 评估 Agent |
| N10 | P2 | 候选交付、状态更新与回退 | 已验收候选 | 主 Agent |

依赖图中 N07 仅是“新纠错”实验前置：N08 的旧可信行为数据 A/B 基线可在 Gate A2 后独立启动。教师 NO_GO 不应阻止已有可信数据上的容量诊断，但也不能让容量实验绕过数据门禁。

多个 Agent 可以并行读代码、写独立测试；公共契约由主 Agent 冻结。数据/表示版本定版之前不要分别生成不兼容的大批产物。并行 Agent 共用一个资源总预算，不是各自获得全部配额。

## N00｜恢复“源码—本机实验—报告”之间的证据链

**目标：** 先确认到底训练到了哪里，再决定哪些实验必须重跑。

**先读：** 根目录两份 v3.0 文档、当前状态文档、`.gitignore`、`package.json`、本机 `training_runs/` 的可用文件与最近运行记录；20260919 原任务书存在时仅作历史参考。

**执行详情**

1. 通读根目录两份 v3.0 文档，记录文件 hash 和版本；读取仓库工程约定。记录当前分支、HEAD、`git status --short`、与审计提交的差异，以及受影响源码/dirty diff 的 hash。参考提交本地不可用时如实登记，不要求自动远程 fetch，更不能 reset 或覆盖未提交改动。
2. 盘点新旧线性、MLP、双头网络模型；原始回放、状态快照、分区文件、T08/T09/T10/T11 报告、训练日志和配置。以文件存在、可读、hash 和实际内容为准，不仅搜索文件名。
3. 对每个训练运行记录：唯一状态数、根家族数、训练样本呈现次数、实际 epoch/optimizer steps、种子、checkpoint、policy/value 指标、自然终局覆盖率、退出状态。缺失填 null/unknown，不填 0 冒充测量。
4. 对每个评估运行记录：模型及教师配置 hash、对手、规则、地图/根家族、先后手、超时口径、原始逐局结果、决策成本。仅有汇总数字而无来源时降低证据等级。
5. 本机已有试验若能重建命令与输入，先选择最小一次复跑；不要把不可追踪实验整体删除。教师是否已资格验收必须引用具体文件。
6. 记录执行机器 CPU、内存、Node/Python、实际可用训练后端与部署目标。不要从显卡型号推断使用 CUDA，也不要把 GPU 利用率当作训练效果指标。
7. 在 `CURRENT_TRAINING_STATUS.md` 顶部增加实际执行日期的段落，分“实现”“实测”“棋力”“1 秒性能”“阻塞”；原历史结果保留。不得把本书日期当作未来运行结果的日期。
8. 检查部署目标是浏览器、Node 还是两者；记录实际 CPU/内存/后端、现有策略入口、worker 配置与已有预算。创建统一实验账本；CPU 并发吞吐评估与在线单局延迟评估分开。

**验收：** 每项“已训练/已通过”可落到证据；当前模型与旧 v4 不混名；没有把旧 27.9% 当新进度；大文件不强行提交 Git。

**交付：** `inventory.json`、`run-index.json`、`task-status.json`、`budget.json`、`evidence-gap.md`、更新后的状态文档。

**降级：** 找不到本机新产物时，后续正确性任务仍可进行；相关实战复现登记 BLOCKED，不编造新网络胜率。

## N01｜修复动作索引、数据完整性与结果视角

**目标：** 所有可训练样本都能说明“哪个状态、谁实际走了什么、教师建议什么、哪个结果可信”。

**先读：** `skirmish_dataset_vnext.ts`、`skirmish_dagger_vnext.ts`、`skirmish_bc_train.ts` 中特征候选接口、`src/game/env.ts` 的固定动作空间编码。

**执行详情**

1. 先写失败测试：构造重标注样本，行为动作在采样数组的位置与其全局固定索引不同；旧迁移必须暴露错配。对标签动作与原行为动作分别核验。
2. 从实际可重建状态恢复固定空间索引，验证 `actionCode ↔ fixedActionIndex` 双向一致。状态缺失则不生产伪造索引；若修改 schema，发布新版本并提供兼容读取/隔离策略。
3. 区分“候选里存在”“动作码结构正确”和“规则引擎证明合法”。缺少引擎验证的历史样本不能把 `legalInFixedActionSpace=true` 当成强保证。
4. 统一训练加载门禁：registered train root、可读取且 hash 匹配的快照、规则/表示版本匹配、合法性和索引正确、结果字段一致。其他分区即便写着 `usableFor=trainable` 也必须拒绝。
5. 将记录身份 ID 与状态内容 hash 分开。校验器从实际字段重建身份输入，并与存储的 seed/ID 交叉验证；另验证快照内容 hash。不要只重算一个可独立篡改的旁路对象。
6. 分开 `perspectivePlayerId` 与 `perspectiveAllianceId`，验证当前采集玩家、结果视角和 winner 的关系。两方 SD 也写 playerId 不等于 allianceId 的单测，避免恰好相等掩盖错误。
7. 自然胜负须有真实终局来源与可追踪续局；超时、停滞和错误不写成自然平局或材料胜。`verified+unknown`、没有引擎终局证据却声称自然胜负等冲突必须拒绝。同一步恰好满足规则终局与外部上限时，先保存两个实际事件，再按预先约定以规则终局决定最终结果；是否将 truncated 单独保留或规范为单一 termination 必须写入 schema，不能因两个条件同时触发就丢掉真实胜负。[R3]
8. 统计已有迁移文件受影响数量；新输出使用新 runId，保留旧文件及失效原因。只重导出依赖错误字段的产物，不无差别重做全部历史数据。

**最小验收用例：** 局部/全局索引错配；只改 behavior 不改 seed；快照被替换；dev/holdout 强行标 trainable；联盟视角不同；自然终局与上限同一步触发的规范化；缺少索引恢复所需状态。

**交付：** `dataset-contract-tests.log`、`migration-impact.json`、`reconstruction-audit.json`、`dataset-manifest.json`、`quarantine.jsonl`。

**停止条件：** 全局索引无法可靠恢复或状态来源不可信时，只保留历史对照/诊断用途，不进入新完整状态训练。

## N02｜统一动作语义并消除关键编码碰撞

**目标：** 不是让每个字符串 ID 都有唯一特征，而是让需要不同战术判断的动作能够区分。

**先读：** `skirmish_action_semantics.ts`、`skirmish_network_features.ts`、`skirmish_navigation_features.ts`、网络特征测试及引擎动作类型。

**执行详情**

1. 网络编码统一调用共享语义层，避免再实现一套目标解析。行动者坐标、真实目标、目标单位、来源城堡与部署落点分别存在；每个可空字段配 presence mask。
2. 写规则可达的碰撞反例：两城堡同兵种招募；同城堡不同部署格；同一单位破坏不同目标城镇；相同属性的不同攻击/治疗/支援目标；相同属性单位从不同起点移动到同一落点。
3. 同一状态枚举完整合法动作，按动作向量分组，记录不同动作码的碰撞。使用规则后果、目标归属或教师近优集合把碰撞分成“可解释等价”和“有害歧义”；不能为达到 0% 把任意 actionCode 哈希塞入特征掩盖语义缺失。
4. 招募价格与指挥官身份使用引擎对应规则函数，不把缺省价格记成 0；区分当前金币和招募后余额。生命值归一化使用与观测/规则一致的有效最大生命值。
5. 实体排序采用语义属性与稳定等价处理，不以 ID 字符串破坏表示不变性。同格 stacked/pending 状态补 ID 重命名与数组置换测试；保留所有必要实体，不用覆盖同格的方式解决。
6. 记录 Top-10 截断覆盖率、地图分箱信息损失、状态类别丢失情况。首轮优先精确 actor/target 局部字段与路径可达性；不立即强制完整 CNN。若关键局面大量依赖被丢失实体/连通性，再做精确网格或实体聚合对照。
7. 状态级目标集合、单位查找、规则价格、必要距离图每个决策只构建一次，候选共享；缓存键含状态/规则/视角，不能跨局污染。
8. 发布明确 `stateEncoderVersion`、`actionEncoderVersion`、字段清单和 hash。输入维度相同但含义变化也必须拒绝旧模型静默加载。保留旧表示作可命名对照。

**验收：** 指定非等价反例可区分；变更经过新特征单测、完整候选审计和编码耗时测量；不使用教师分数、结果、seed、来源文件/分区/模型身份作预测特征。

**交付：** `representation-v2.md`、`action-collision-audit.json`、`encoder-parity.json`、`encoder-profile.json`、新模型兼容策略。

**停止条件：** 关键正负动作仍不可区分，或同一输入训练/部署编码不一致，不扩大训练轮次。

## N03｜修复搜索集合、真实对手阶段和预算语义

**目标：** 让“搜索了什么”和“看过对手”成为可检查的事实。

**先读：** `skirmish_search_teacher.ts`、预筛选测试、`src/game/ai/planning.ts`、对应规则层 pending/回合测试。

**执行详情**

1. 把候选流水线分成可观察阶段：完整合法集、轻量预筛集、多来源池、最终 beam、实际展开集、完整评估集。每个动作记录来源、保留/淘汰理由；新增网络候选最终存活率。
2. 最终 beam 使用显式的关键动作保护与来源配额，而非加入多来源后再统一按启发式截断。明确保护动作超过预算时的优先级、溢出和回退；不能一面硬截断一面宣称全部保留。
3. 设计允许网络建议从粗筛外进入的独立配额，或明确网络只重排哪些集合。成本纳入全局计量，不能隐藏“对所有合法动作评分”的开销。
4. 为 win-now 建立可审计保障范围：可低成本覆盖的终局动作类别先做规则判定；复杂组合仍由搜索负责并承认有限覆盖。补最终 beam 之外存在胜着的测试，不宣称有限宽度全局最优。
5. 修正预筛招募限流语义：若保留所有合法招募是有意安全选择，就明确其 soft cap；若要求真正限流，必须按兵种/城堡/战术例外分层并测召回，不能直接删除困难动作。
6. `handToOpponent` 返回明确的 `reached-opponent / terminal / incomplete / budget` 类状态。没有真实切换到敌方时，不允许按对手视角调用回应评估；pending 必须通过合法动作推进或标 incomplete。
7. 每个候选独立记录回应玩家/联盟、底层动作数、是否出现攻击/占领/其他有效改变、是否完成期望阶段、是否截断。决策级 `selectedSawOpponentPhase` 从选中候选生成，不能取整次搜索任意分支。
8. 默认一个对手动作只是基准档。增加有上限的“移动→攻击/占领”等有效回应档及至少一个更深参考档。完整回合昂贵时不强制全回合，但将浅层盲点明确计入教师资格。
9. 真实 terminal 值始终优先；截断评价、完整评价和未评候选分开，不把没有对手证据的高估值当强标签。搜索 margin 只在同一评价口径下有意义。
10. 统一计时接口，记录候选生成/评分/克隆/推演/叶评价各阶段成本。同步长调用无法抢占时明确超时余量；online 使用 worker/watchdog 等机制前先核实运行环境。
11. PV 保存全部实际动作，包括中间续着与交接，逐步重放得到相同叶状态 hash。保存父节点/兄弟节点不变性测试。

**关键验收：** 低启发式分的网络候选能按配额留下；保护动作不被最终 slice 悄悄抹掉；pending 不冒充对手；selected-candidate 有独立证据；对手先移再打案例不被“一步看过”糊弄；节点/时间/动作深度彼此分开。

**交付：** `candidate-survival.json`、`search-contract-tests.log`、`phase-coverage.json`、`search-traces.jsonl`、`search-profile.json`。

**停止条件：** 对手身份或终局语义错误时停止教师标签生产；仅性能不达标时可以保留为离线教师候选，不能直接当在线 AI。

### N03-A｜落实 1 秒端到端决策，而不是只改一个参数

**接口状态：以下为待实现/适配的契约，不是当前仓库已有 CLI。**

- 在线 profile 命名为 `online-1000ms-v1`。推荐初始 `deadlineMs=1000`、`searchStopElapsedMs=850`、`returnTargetElapsedMs=950`；全部相对同一个调用方 t0。850/950 是可预检调整的余量，不是额外预算；线上上限 1000 不得悄悄扩大。
- t0 位于调用方接受决策请求、开始状态复制或排队之前；t1 位于调用方收到且完成合法性校验、可以提交给引擎时。请求传输、特征准备、队列等待、候选全量评分、worker 往返、回退均包含在 t1-t0。
- 模型可在接受决策前常驻加载/预热，启动耗时另报；若在首个请求里懒加载，就必须计入该请求，不能删除首局/首步数据美化结果。
- 使用同一可注入单调时钟，适配 Node/浏览器；跨 worker 不能盲目共享不同时间基准的绝对毫秒，传递可核对的绝对期限或扣除排队后的剩余预算。子树/候选/重试不能各自重新获得 1 秒。
- 在昂贵阶段之前和可拆分批次之间检查剩余预算。逐步保留已校验合法的 incumbent（当前可用答案）；没有完成推演时保留已有合法基线动作。优先结束已证明的即时胜着，不让简单局面故意等满一秒。
- 采用有上限的增量探索：先让不同来源候选取得可比较的最低回应覆盖，再把剩余预算用于有希望的分支。不要让第一个候选耗尽全部预算；也不要把未经对手检验的乐观分与完整最差回应分直接等价比较。
- worker 使用实际运行端支持的实现；Node 可采用常驻 `worker_threads`，浏览器采用对应 Web Worker。不要为每个动作创建新 worker，不把所有同步计算放在主/UI 线程后再依赖同线程 setTimeout“强制抢占”。官方文档说明 worker 适于 CPU 密集工作、复用可避免反复创建开销；定时器没有精确触发保证。[R4][R5]
- 主线程/调用方预先持有与该请求局面绑定的合法兜底动作。watchdog 达到 return target 或 worker 错误时，可直接返回该动作；不能在超时发生后才启动一个昂贵启发式重新计算。取消/终止 worker 不阻塞动作返回；必须记录停止确认与残余消耗，防止无用计算无限累积。
- 每个请求包含 `gameId`、`requestId`、`stateVersion/stateHash`、`playerId`、模型/配置 hash。新局面到来、撤销、重开或模型切换后，旧响应一律丢弃；不得在旧搜索结束时把动作应用到新状态。
- 回退的合法性必须由当前请求的真实合法集合确认，必要时在提交点复核；可以复用已经生成的合法集合，但不能通过绕过规则降低延迟。搜索质量回退、异常回退和非法提案分别计数。

**资源约束不能相互冒充。** `nodeBudget` 控制生成的节点，不能限制任意一次候选评分的耗时；`prefilterWidth` 可能是软上限，不能当真正评分条数。实际记录节点、候选评分数、规则动作应用数、clone、路径计算、网络前向与缓存命中。离线教师保留单独有限 profile，可以超过在线 1 秒，但须计入离线总预算，且不能冒充在线性能。

### N03-B｜预算与回合语义验收用例

| 用例 | 必须验证 |
|---|---|
| 单个/明显唯一可用动作 | 提前返回；不为了“思考 1 秒”空等 |
| 首次加载、最大候选集、高单位数 | 原始端到端耗时被保留；不只测空棋盘 |
| 候选评分中途超预算 | 不启动新的大批次；调用方有合法 incumbent/回退 |
| worker 错误、死循环模拟、消息晚到 | 及时回退、取消/清理记录；旧响应不会二次应用 |
| 排队和并发压力 | t0 从接受请求起算；不把排队时间排除 |
| pending/堆叠招募无法直接 end_turn | 按合法动作推进或 incomplete；不能伪造已到对手 |
| 对手需要 move→attack / move→capture | 指定反例中实际覆盖组合；动作层数不冒充完整回合 |
| 预算分摊 | 各候选没有单独重置一秒；选中候选有自己的回应证据 |
| 极慢或不支持的平台 | 标 PERF_HOLD，保留合法回退；不能伪造“绝对永不超时” |

单测使用可控时钟验证逻辑，真实平台测量验证延迟。通用操作系统/JS 运行时不是硬实时系统；“1 秒”是用户要求的工程验收目标，不是数学上的调度保证。记录所有超限及原因；不得把阈值事后改成 1.2 秒后声称通过。

**新增交付：** `online-budget-contract.md`、`online-profile.json`、`deadline-tests.log`、`worker-isolation-tests.log`、`decision-telemetry.jsonl`、`latency-report.json`。


## N04｜强化训练数值、模型契约与部署推理

**目标：** 让小网络可验证地训练、恢复、加载，而不靠随机初始化和静默补零掩盖错误。

**先读：** `skirmish_dual_head_net.ts`、双头网络测试、`skirmish_neural_search.ts`。

**执行详情**

1. 给手写反向传播增加小网络有限差分检查，避开 ReLU 不可导点；分别检查策略路径、价值路径和共享 trunk。检测 loss/gradient/parameter 非有限值。
2. 明确损失公式。推荐先采用分别归一化：

```text
L_policy = Σ_i w_i · CE_i / Σ_i w_i
L_value  = Σ_i m_i u_i · (v_i - z_i)^2 / Σ_i m_i u_i
L_total  = L_policy + λ_value · L_value
```

其中 m 为可靠价值目标掩码；有效分母为 0 时，该项无新梯度并报告 `valueCount=0`。保留按全样本 N 归一化也可以，但必须写清公式并报告 M/N 和有效权重，不能使用两种口径却不解释。

3. 测试有/无价值标签混合批次及已有动量后的全掩码批次。区分“无新价值损失梯度”和“参数完全不变”：共享 trunk 的策略更新和历史动量可能改变预测，不能把两者混为同一个承诺。
4. 模型 schema 验证全部必要权重、维度、张量长度、有限数、表示版本、规则兼容声明、标准化状态、内容 hash。缺字段必须失败，不能用随机初始化补全后继续运行。
5. 标准化统计只来自 train；验证 sigma 非零且有限，近常数列采用预设处理；部署折叠与训练 checkpoint 分离，阻止重复折叠；记录容差一致性，不把浮点 `toBeCloseTo` 写成严格逐位相同。
6. 保存优化器/动量、采样 RNG、训练步数、配置和数据 manifest；恢复后与不中断训练比较。只保存权重的部署模型不能宣称精确续训 checkpoint。
7. 独立实现不分配梯度数组的 inference-only 路径；评所有候选共享 state embedding，避免重复编码地图。先剖析再考虑推理框架迁移。
8. 网络输出非有限值、版本不匹配、形状错误时触发具名错误与已验证策略回退。禁止叶评估器静默归零；需要让上层知道回退原因和次数。
9. 保留已有合成过拟合测试；再增加经过 N01/N02 验证的真实小批状态测试。微型记忆只能作为可学性/实现检查，不能选模型晋级。

**验收：** 梯度/掩码/恢复检查通过，缺字段和非有限模型被拒绝，编码与部署分数在预设容差内一致；回退可观察，纯推理热路径不分配训练梯度。

**交付：** `gradient-check.json`、`loss-contract.md`、`resume-parity.json`、`model-schema.json`、`inference-parity.json`、`model-loader-tests.log`。

### N04-A｜支持可配置深度，但不重写已有引擎

参考实现的 `DualHeadSpec` 只有 `hidden/embed/policyHidden/valueHidden`，层数与张量名称固定；所以 NET_B 不是给现有 CLI 传 `--depth 3` 就能得到。[S10][S11]

1. 先检查本机是否已有等价多层实现。没有时新增有版本的隐藏层数组描述，例如 `trunkHidden: [256,256,128]`；前向、反向、初始化、动量、checkpoint、参数计数、标准化折叠和 inference-only 全部从层表派生，不只在前向插一层。
2. 保留旧模型严格加载器与旧表示，不把旧 v1 checkpoint 静默升级为新深度。新模型发布独立 `kind/schemaVersion`，元数据含实际层表、激活函数、输入表示版本、参数量与训练来源。
3. NET_A、NET_B 必须使用同一新版实现路径，避免把“新数值实现”和“多一层”的效果混在一起。旧模型保留为 named legacy reference。
4. 层表允许的范围有限并经过参数校验；拒绝空干路、负宽度、非有限配置和超资源大小。A/B 的激活、优化器、候选头与损失先保持一致。
5. 分别对 2 层、3 层小维度网络做有限差分；验证每一层更新、保存/加载、恢复训练以及部署容差。旧固定层网络与同形状新版在指定相同权重下做前向对齐，不能比较两个不同随机初始化却宣称兼容。
6. 残差块、LayerNorm、Adam 等都不是本轮加深的强制伴随变化；需要时单独成实验。不能用更换激活函数来掩盖 NaN、梯度异常或标准化错误。

**建议 schema 形状（待实现，输入版本由 N02 填充）：**

```json
{
  "kind": "skirmish_dual_head_v2",
  "schemaVersion": 2,
  "architectureId": "NET_B",
  "stateEncoderVersion": "RESOLVE_FROM_N02",
  "actionEncoderVersion": "RESOLVE_FROM_N02",
  "trunkHidden": [256, 256, 128],
  "policyHidden": [64, 64],
  "valueHidden": [64],
  "activation": "relu",
  "valueEnabled": false,
  "stateDim": null,
  "actionDim": null,
  "parameterCount": null
}
```

null/占位符必须在实际产物中替换；严格加载器拒绝未填的示例。策略头输入是最后一层共享表示与新动作特征拼接，最终输出标量 logit；价值输出 tanh 仅在有有效监督和质量资格时用于搜索。`valueEnabled=false` 不代表给所有状态强制赋价值标签 0。

**新增交付：** `architecture-schema.json`、`depth-shape-tests.log`、`depth-gradient-check.json`、`legacy-model-parity.json`、`parameter-cost-report.json`。


## N05｜冻结能回答问题的评估协议

**目标：** 把模型收益、规则辅助收益、搜索收益和多算时间的收益分开。

**先读：** 历史 endgame 报告及策略工厂、现有 iteration/multiscenario eval 工具；原任务书存在时可补读 T03，本节已完整重述本轮评估要求。

### 数据与局面分区

根家族的全部派生快照、回放、镜像/角色变换和续局继承同一分区；不要逐行动随机切分。训练、开发回归、最终留出在首次看结果之前冻结。已有数据是否覆盖某张地图必须登记；只换种子的测试叫“同地图新种子”，不能冒充“未见地图泛化”。来源不明的家族隔离。

### 必需对照

先建立以下基线，缺旧模型则明确降级，不伪造复现：

| ID | 策略 | 用途 |
|---|---|---|
| B0 | 原启发式 | 基本对手与运行成本 |
| B1 | 无 BC + 与历史组合完全相同的目标/历史/收官辅助 | 解释辅助贡献 |
| B2 | 原纯 v4 | 历史模型参考 |
| B3 | 原 v4 + 同样辅助 | 复核历史组合 |
| B4 | 修复后线性/MLP 候选 | 判断轻量学习是否已足够 |
| B5 | 当前纯网络策略 | 测网络独立棋力与时延 |

目标、历史、撤退、城镇保护等开关不得混为一个“记忆贡献”。未实现正交开关时，报告“联合辅助”而非强行归因。

N09 的搜索对照采用固定同一网络的 2×2 设计：S00 原叶评价/无网络先验；S10 网络先验/原叶评价；S01 无网络先验/网络价值；S11 网络先验/网络价值。纯网络 B5 不含搜索；函数名的 `1-ply` 不应被解释为真实施加一步引擎的搜索。

### 预算与指标

做两个独立比较面板，不混为一种“同预算”：

- **节点约束面板：** 固定节点数、候选/阶段协议；时间限制不能悄悄决定一部分动作。时间限制触发的样本另列，保留安全硬上限。
- **在线时间面板：** 主档固定为同一实测平台、同一资源限制、每个底层动作端到端 1,000 ms；按 N03-A 计入请求和回退全流程。报告实际完成节点数，加载/预热与首次请求分开披露。更快的评估器可以在剩余预算内展开更多节点，这是完整系统收益。可增加 250/500 ms 的低成本曲线点，但不能替代 1,000 ms 主验收，也不能自动把实验格数翻倍。

至少报告正常开局、优势/劣势残局、水路/路径困难局面；对先后手和对手分层。Icy Paths 保留为开发弱项，不再把已经观察/调参的旧局面称为封存测试。

主指标为自然胜局数/实际启动对局数，同时独立列自然负、规则平局、maxPlies/maxSteps、停滞、运行错误、非法动作、回退、决策 p50/p95/p99、每回合与全局耗时。运行错误不能悄悄当作平局或从分母删掉；基础设施问题导致比较无效时登记 HOLD 并保持原记录。

配对换先后手，共享根家族不是独立样本。置信区间或重采样按根家族聚类；样本太少就给区间与 HOLD，不能把反复重跑同一 seed 当成新增独立证据。阶段、对手权重和晋级条件预先固定；最终留出不参与调参。

**验收：** 同一命令能输出逐局记录与汇总；分区没有家族泄漏；成本口径统一；每个改动能对应具体对照。

**交付：** `split-manifest.json`、`eval-protocol.json`、`promotion-policy.json`、`baseline-matrix.json`、`budget-profile.json`。

### N05-A｜冻结深度、搜索和时间的实验矩阵

**先后次序：** 先 NET_A 与 NET_B 的纯策略/统一辅助和同搜索配置对照，再对保留候选做 S00/S10/S01/S11。不要第一轮就运行“3 种深度×3 种学习率×4 种搜索×3 种时间×3 个种子”的全组合。

A/B 先共享同一数据、样本权重、分区、候选协议、训练步数/数据呈现预算、初始化种子清单和优化器。相同 seed 用于配对管理，不意味着不同形状的初始权重相同。A/B 增益同时包含参数量变化，报告名称写“架构 A/B 效果”；要把效果归为纯深度，必须另做参数量近似匹配的两层加宽对照。

固定节点面板还须披露实际节点数、候选评分数与深度覆盖；网络先验可能改变树，因此不是保证完全相同搜索树。若安全 wall cap 触发，该样本不再满足预设节点比较，应单列或在两臂一致设置下重做，不能隐去。

在线主面板在目标用户平台、前台活跃部署状态、无并行训练的标准负载下运行；Node 与浏览器分别报告。批量 8-worker 对局只能用于吞吐/筛选，不能把其竞争下延迟或服务器延迟冒充客户端 1 秒性能。压力测试另列，不删慢样本。

### N05-B｜最低遥测与晋级口径

每个决策至少保存：run/game/request/state 标识；策略与模型/配置 hash；根候选数、各层保留数、实际评分/展开数；selected-candidate 对手覆盖；编码/网络/搜索/返回耗时；完整 `endToEndMs`；是否冷启动；回退类型、原因；deadline 是否超限；陈旧响应是否丢弃；取消后残余计算是否停止。阶段计时应标同步/并行关系，不能简单相加造成重复计时。

每个对局保存：起始根、角色/先后手、对手、结果与终止原因、总动作数、自然胜负、非法提案/最终非法动作、总决策耗时、玩家回合内 AI 累计耗时、平均/最大连续等待。每动作一秒可能累计成很慢的回合，必须报告；本书不把整回合也擅自限定为一秒。

**本轮建议验收规则，必须在看正式对局前写入 promotion-policy：**

- 正确性：指定关键语义/索引/阵营/终局测试全通过；部署实际提交的非法动作和应用陈旧响应为 0。出现一次即停止相关候选晋级。
- 在线时延：在预注册目标平台的正常验收样本中，所有决策记录齐全且 `endToEndMs<=1000`，报告 p50/p95/p99/max 和超限数量；出现超限则在线 Gate 暂不通过，优化/回退后另 run 重测。有限样本零超限不是永不超限的证明，必须披露样本量与压力测试。不能只以 p95 合格代替上限要求。
- 回退：不得靠总是回退把弱网络包装为强模型。正常预算回退、异常回退、模型自身作出的决策分别统计；晋级评估比较完整部署策略，纯模型贡献另报。允许合法安全回退，但大量回退要说明实际是谁在下棋。
- 质量：使用预先指定、固定参考策略与分域权重，按根家族聚类的配对差值及 95% 区间。建议开发筛选实用提升门槛为自然胜率点估计 +5 个百分点；它只是筛选门槛，不是统计显著性的替代。
- 正式质量晋级：目标主指标的差值区间下界 >0，并通过关键域预设非劣要求；或质量差值区间下界 >=-2 个百分点且达到预注册的明确成本下降目标（建议中位决策成本至少下降 20%，同时尾延迟不恶化）。本行数字为本书建议，可在预检后、正式结果前调整并冻结；结果后不得放宽。
- 区间太宽、自然终局太少、根家族不足或数据权重无合理依据时输出 HOLD；没有足够样本判断 5 个百分点很常见，不能把 24 局筛选写成强结论。最终评估样本量由精度目标与预算定；预算不足就不宣称正式晋级。

节点数与延迟是诊断指标，不是棋力。S10/NET_A 最终胜出也是合格结论；不用强制选择 NET_B 或 S11。

**新增交付：** `experiment-registry.json`、`architecture-ab-plan.json`、`online-1000ms-eval-plan.json`、`turn-latency-report.json`、`paired-cluster-analysis.json`、预冻结 `promotion-policy.json`。


## N06｜让教师先取得资格，再生产大量标签

**目标：** 区分“搜索代码合法”“搜索解决某类战术”和“搜索整体比参考强”。

**执行详情**

1. 锁定 N03 修复后的教师版本、候选规则、叶评价、预算与对手。保留旧搜索配置作为对照，不覆盖旧资格记录。
2. 先过规则夹具，再做正常/残局/路径难题的有限对局。与 B0、B1 及已有最强可复现组合比较；只打败弱基线不自动成为总教师。
3. 同时量化完整候选覆盖、选中候选对手回应完整率、预算截断、回退和每条可靠标签成本。高 fallback 搜索应单独分析实际由谁决策。
4. 对教师/学生分歧状态，教师比较中必须显式加入学生动作，即便它不在教师最终 beam；否则不能写“教师值减学生值”的可靠 margin。两动作使用匹配的续着、回应完整性与预算；采用公平分摊避免后评动作被预算饿死。未评出学生动作或评价口径不同则 margin=unknown，不随手赋 0。
5. 关键战术夹具给局部资格；自然完整对战给域内资格。若正常开局不稳定而残局有效，发布仅限残局的教师，普通开局保留原策略/其他已验证教师。
6. 对疑似收益最大的失败类型做少量配对续局，确认是否只是人工估值偏好，防止弱教师把学生纠正坏。

**决策格式：** `GO / LOCAL_GO / HOLD / NO_GO`，附 domain、configHash、dataSplitHash、qualityEvidence、costEvidence、局限。

**验收：** 相对于冻结参考达到预设质量或质量—成本条件，且不含语义错误。24 场筛选不能被包装成“已证明提升 5 个百分点”；精度不足就是 HOLD。

**交付：** `teacher-qualification.json`、`teacher-eval.jsonl`、`teacher-failure-cases.md`、`teacher-query-cost.json`。

**降级：** 教师仅有局部价值就局部使用；完全没有收益则先交付更可靠的基线，不为完成原 T09 强制使用无资格标签。

## N07｜小规模 DAgger 纠错数据与可靠结果轨迹

**目标：** 学习学生自己走入的局面，不只是重复已有教师回放。

**执行详情**

1. 复用 `skirmish_dagger_vnext.ts`；补可复现采集入口，而不是把映射函数存在当成采集已经完成。
2. 固定本轮学生、教师和对手 hash，从 train 根家族滚动。优先采集实际失败前后、重复移动、招募/经济失衡、难以收官及关键防守状态；同时保留正常行为样本，避免只学习异常分布。
3. 记录实际行为动作和反事实教师建议，不让建议伪装为已经执行。教师查询的资格、domain、配置、候选覆盖、预算和完整性随样本绑定。
4. 对一致样本也保留教师查询状态与资格；仅用 teacher=null 不能区分“教师同意”和“没问/问失败”。扩展元数据，不破坏已知行为轨迹。
5. 高置信纠错、近优多动作、证据不足分别标记。教师 margin 不是概率；不把 beam 分数随意当 MCTS 访问次数。首轮可用可靠硬标签，软目标作为后续受控对照。
6. 样本重复按 state 内容 hash 审计，并保留根/轨迹身份；控制单局大量相似行动占比。聚合旧合格数据与新纠错数据，混合比例通过少量开发对照选择，不提前认定 25% 或越多越好。
7. 保存完整快照和真实续局结果。胜、负、平都可提供有定义的状态价值目标；截断没有可靠目标时掩码，不用材料差补胜负。
8. 根轨迹结果只能标注其实际执行策略下的状态价值目标，不能给未执行的教师动作伪造 Q 值或续局胜利。行为策略变动、对手变化随轨迹存档，防止混合结果被误称最优价值。
9. 第一轮结束先训练/评估；只有 Gate C2 支持继续时再作第二轮。禁止无限采集或只凭训练 loss 降低持续扩量。

**验收：** 每条训练样本可定位根、快照、行为、教师证据、分区与结果；分歧数、可靠纠错率、重复率、隔离率和终局覆盖齐备。

**交付：** `dagger-round-01/manifest.json`、`capture-config.json`、`teacher-queries.jsonl`、`outcomes.jsonl`、`aggregation-report.json`。

## N08｜受控训练：网络深度对照、数据增益与有条件价值头

**目标：** 回答“是否需要增加网络深度”，而不是先假定三层一定更强。把表示、深度/宽度、纠错数据和价值目标分开检验。

**依赖：** Gate A2、N05；使用可信已有行为数据时不依赖 N07，加入本轮纠错时必须先完成 N06/N07。旧行为数据可以作为受监督基线，但不能因为可读取就冠名强教师数据。

### N08-A｜先复核三个层次的可学性

1. 保留合成小数据测试，检查数值与反向；它不代表训练游戏能力。
2. 从 train 分区选择 32—64 个真实、无有害动作编码冲突、标签可靠的状态做微型过拟合。此 tiny run 可使用不同于正式训练的步数，但必须记账；不把记忆这些状态当泛化，不让它们进入最终 holdout。
3. 正式基线使用独立根家族训练/验证。报告 train/dev 学习曲线与完整合法候选指标；train 不足、dev 不足、搜索不使用网络三个问题分开诊断。3 个 epoch 只是起步探针，尚未收敛不能直接宣判容量不足。

### N08-B｜主要架构与条件扩展

下表只定义隐藏层，最终策略标量输出和价值 tanh 输出不计为隐藏层。

| ID | 共享干路隐藏层 | 策略头隐藏层 | 价值头隐藏层 | 执行规则 |
|---|---|---|---|---|
| LEGACY | 从现有模型 spec 读取 | 原样 | 原样 | 仅保留历史参照；不猜 checkpoint 实际参数 |
| NET_A | 256 → 128 | 64 → 64 | 64 | 本轮修复后中等容量基线 |
| NET_B | 256 → 256 → 128 | 64 → 64 | 64 | 本轮主要“适度加深”候选 |
| NET_C | 256 → 256 → 256 → 128 | 64 → 64 | 64 | A/B 支持继续且预算允许才运行 |
| NET_A_WIDE | 两层，按参数计数选择接近 NET_B 的宽度 | 与 A/B 相同 | 与 A/B 相同 | 仅用于深度/参数量归因；未运行就承认不能纯归因 |
| SPATIAL_EXPERIMENT | 精确网格＋实体；可从 4 残差块/64 通道作设计起点 | 独立定义 | 独立定义 | 仅表示审计证明关键瓶颈时另立实验，不在 A/B 中偷换 |

NET_A/B 都使用 N02 修复后的相同编码器，最终输入维度自动从版本化配置读取；不要为了保持 356/32 不变压掉新增语义。参数量、checkpoint 大小、状态前向和每候选头耗时由代码实际计算/测量，不凭层数估计性能。

当前结构一次状态干路服务多候选，因此新增容量优先放共享干路，保持动作头轻量；这只是本轮工程选择，最终收益由实测决定。残差结构可参考 [R6]，但论文不是本游戏的胜率保证。

### N08-C｜分阶段运行，避免不可归因的组合爆炸

**E0 基线登记：** 复用或最小重跑原启发式、无 BC 同样辅助、可获得的纯 v4/组合、修复后的线性/已有 MLP。已有可复验证据复用；不存在的旧产物记 BLOCKED，不无限追索。

**E1 深度主对照：** 用同一可信冻结数据、同样 sample weighting 和训练目标训练 NET_A/NET_B。第一轮都为 policy-only：`valueEnabled=false` 或价值损失/搜索接入关闭；不使用随机价值输出。首个 seed 作学习曲线与性能预检，资源足够再补预注册的另两个 seed；不能只公布获胜 seed。

**E2 纠错数据效果：** 教师合格并完成 N07 后，选定一种架构，比较“原合格训练数据”和“原数据＋本轮纠错”。新数据比例先限定一个预注册值，必要时最多增加一个对照；不同时改变深度、优化器和搜索配置。此前历史 25% 并非最优证据。

**E3 价值头：** 仅在自然终局来源可靠且 train/dev 均具有足够根家族、阶段与正负/规则平局覆盖时，给选定架构训练联合价值版本。数据不足则保持策略先验＋原评价，输出 VALUE_HOLD。不让全胜或全部截断数据产生伪校准价值。

**E4 条件扩展：** B 获益但深度归因不明，可在剩余 fit/呈现/对局预算内选 NET_A_WIDE；明显拟合不足且微型测试/数据可信时可选 NET_C；关键连通性被编码抹掉时才启动 SPATIAL_EXPERIMENT 设计/小探针。只选最能消除当前不确定性的一项，不自动全部执行。

### N08-D｜训练预算和优化约束

1. 参数、初始化 seeds、训练样本呈现数与优化器在正式对照前冻结。推荐先 3 个完整数据遍历；若学习曲线仍改善、未过拟合且未达到全局预算，可在预注册上限内继续，默认每个正式 fit 最多 12 个完整遍历。不能让 A 训 3 轮、B 训 12 轮后称公平深度比较。
2. A/B 第一比较采用相同样本呈现/更新预算；训练消耗另报。确需各架构单独调学习率时，预注册相同调参额度，报告“各自优化后配置对照”，不能把调参差异全归为深度。
3. 默认保持已有经过 N04 检验的优化器。梯度裁剪、标准化、batch、shuffle、early stopping 等配置必须一致或明确消融；新增一层不同时换优化器、激活和候选采样。
4. 记录唯一状态、根家族、样本呈现总次数、实际更新步数、每 epoch 耗时、有效价值目标数/权重、保存的最佳 checkpoint。不同 seed 和失败运行同样计入总预算。
5. train/dev 分开；标准化只用 train。验证指标用于早停和候选筛选，正式模型选择以预冻结独立开发对局及在线表现为准。
6. 不要求把全部几十万/百万旧特征直接送进新网络。旧不可逆哈希只能用于相应历史基线；需要完整状态的新编码必须从可回放状态重建，并通过契约。
7. 首批继续支持 CPU smoke。实际手写 CPU 训练无法满足预算时，记录瓶颈，可降低首批样本或使用项目实际支持的训练后端并单独对齐；不复制规则到 Python，不默认 AMD 卡可用 CUDA，不把 GPU 利用率当成果。
8. 若更大模型因数据/优化不足未收敛，结论为该预算下未获得收益，不宣称深度在任何条件下无效；同时不以“未来可能有效”为理由无限增加 epoch。

### N08-E｜模型指标与决策表

策略指标包括完整合法候选 top-1、近优动作命中、分动作类型的错误率、正负候选 margin、关键战术错误、纯策略与统一辅助部署下自然对战；抽样候选准确率单独报告，不混为完整动作表现。

价值指标与常数、材料/领地基线比较，按局面阶段、阵营视角、自然胜/负/规则平局分层；tanh 是约定结果回归量，不自动等于胜率。轨迹结果是行为策略/对手分布下的状态监督，不给未执行教师动作伪造 Q 值。

| 实测状态 | 下一步 |
|---|---|
| 真实 tiny 数据都无法拟合 | 回查动作碰撞、标签、掩码、数值和梯度，不先堆层 |
| tiny 通过，正式 train/dev 均不足 | 在冻结预算内测试 B/加宽；也检查数据覆盖与优化 |
| train 高、dev 低 | 优先泛化和采样/正则诊断，不盲目继续加深 |
| B 离线好、1 秒在线差 | 查网络开销、搜索有效覆盖与评价偏差；允许保留 A |
| B 多 seed、分域、1 秒下均有可信收益 | B 进入候选冻结，不自动更换默认 AI |
| A/B 区间重叠且预算不足 | HOLD；保留更可靠/较低成本的已验证版本，不制造统计结论 |

**验收：** A/B 至少有可追踪训练、实际层数、完整候选与对应预算下对战证据；未能运行的 seed/分域写 NOT_RUN。可选择 A、B、已有 MLP 或仅局部候选，不要求 B 必须获胜。

**交付：** `architecture-comparison.json`、`architecture-specs/`、`train-config.json`、`learning-curves.json`、`candidate-eval.jsonl`、`model-card.md`、`value-validation.json`、`checkpoint-manifest.json`、`depth-decision.md`。

**停止：** 关键表示错误、梯度失败、全候选性能异常、数据泄漏、达到账本配额或受控实战退步。先保存原因与可恢复产物，再停止扩量。

## N09｜网络辅助搜索的 2×2 消融与质量—成本验收

**目标：** 证明网络改变了实际搜索结果，而不只是调用次数增加。

**执行详情**

1. 先用 NET_A/NET_B 在相同规则搜索配置下比较，优先 S10（策略先验＋原叶评价）和纯策略；再对保留且价值监督合格的同一个联合网络执行 S00/S10/S01/S11。四臂中“关闭 value 用途”是推理开关，不重新训练四个不同模型；策略-only 与联合训练的影响另立 E3 对照。价值未合格则 S01/S11=DEFERRED，不使用随机头凑满矩阵。每臂保护规则和非网络辅助相同。
2. 从微基准/固定状态集先看：网络建议进入最终 beam 的比例、真正展开次数、叶评价次数、动作改变率、wall time、内存分配和回退。网络完全被剪掉时先修接入，不能直接推断“网络无用”。
3. 再进入冻结的配对开发对局。固定节点面板与端到端 1,000 ms 面板分别报告；前者诊断受限工作量下的信息质量，后者决定真实在线选择。在线候选生成/模型排序/worker 通信同样占预算；不要只统计 searchTeacherAction 返回的内部 elapsedMs。
4. 价值头如拖累搜索，保留 S10，不强制 S11。网络先验没有收益但价值有收益，可以保留 S01；两者都无收益则不宣传专家迭代成功。
5. 在正常/残局/水路/先后手/对手类型上展示质量—成本取舍。报告 1 秒超限比例、max 和整回合累计耗时；p95 合格不代表上限合格。没有预设整回合硬限时不能自行编造一个，但必须披露累计等待并通过提前返回、复用缓存等减轻问题。
6. 只有更强或达到质量非劣且显著更低成本的搜索版本，才进入下一轮教师候选；新教师必须绑定重新核验的域与配置。

**验收：** 能明确回答“策略先验带来什么、价值头带来什么、组合有没有净收益、成本是否可接受”。结论可为局部 GO 或 HOLD，不强制得出正收益。

**交付：** `neural-search-ablation.json`、`quality-cost-frontier.json`、`selected-candidate-traces.jsonl`、`gate-c2-report.md`。

## N10｜可选择交付、回退与有界迭代

**目标：** 交付最强已验证的可运行候选，而不是不断增加未验收模块。

**执行详情**

1. 完成开发候选选择后锁模型/参数，只运行一次预注册的最终留出评估流程。发现问题可回到开发，但原 holdout 已消耗，后续不得继续称“未见”。
2. 核对浏览器或 Node 部署依赖边界；训练模块的 fs、数据导出副作用不得进入浏览器推理路径。worker、缓存、目标/历史记忆按对局隔离。
3. 提供可选择候选、版本显示、模型加载失败/非法输出/超预算回退、结构化诊断。目标端的在线 1 秒验收须独立完成，服务端吞吐不能代替浏览器验收。默认策略不自动切换。
4. 更新状态首页、模型卡和最小复现命令；旧模型与失败模型保留。人工评测作为补充，不代替已冻结的完整矩阵。
5. 当前闭环明确通过后，只有预算文件事先允许第二轮且有足够剩余资源才执行；否则仅交接第二轮计划。冻结对手池含原启发式、无 BC 辅助、当前冠军及可获得历史版本；候选不立即替换所有对手，不在中途改变对手池偷取优势。
6. 两方 SD 尚未通过质量/预算验收时，三方、四方、联盟扩展和从零 PPO/大 Transformer 继续 DEFERRED。它们是独立后续研究，不是本轮捷径。

**交付：** `promotion-record.json`、`final-eval.jsonl`、`deployment-checklist.md`、`rollback.md`、更新的 `CURRENT_TRAINING_STATUS.md`。

## 4. 资源预算、执行批次与停止规则

### 4.1 本轮默认资源边界

以下为本书建议的有限本地执行上限，不是耗时预测、GPU 性能承诺或收费服务授权。N00 优先读取用户已有明确预算，更严格者优先；机器资源不足时可以下调。看正式结果之前将实际上限写入账本，之后不得为了某候选获胜而无上限扩量。

| 项目 | 默认上限/起步档 | 备注 |
|---|---|---|
| 初始对局 smoke | 新启动对局总计 <=8 | 并入全轮 512，不是额外配额 |
| 全轮新启动游戏 | <=512 | 采集续局、教师资格、开发、所有 arms/seeds、最终评估均计数；失败/重试也计入 |
| 最终留出预算预留 | 上述配额中先预留最多 128 场 | 不是固定统计样本量；精度不足 HOLD，不要求凑满 |
| 首轮教师查询 | <=2,000 个状态查询 | 同状态不同教师/配置的重复查询分别计费 |
| 训练唯一状态 | 新小闭环 <=30,000 | 可以先 5,000—10,000；按真实根/阶段抽样，不截取文件前几千行 |
| 正式模型 fit | 全轮 <=8 | 架构、数据、价值、seed、失败重试共享；不是每种架构各 8 次 |
| 正式训练呈现次数 | 累计 <=1,200,000 state-presentations | 每样本每次进入优化均计；多 seed/重复采样都计入 |
| 每 fit 数据遍历 | 起步 3；预注册最大 12 | 早停可少于上限；A/B 比较预算匹配，不能只让 B 多训 |
| tiny 可学性 | 真实样本 <=64；单配置优化步 <=2,000 | 单独记录预算和候选数；合成测试不冒充游戏训练 |
| 延迟探针 | 全轮非对局决策调用 <=2,000 | 固定状态重复可测噪声，但不增加质量证据的独立根数量 |
| 初始化 seed | A/B 目标各 3 个 | 首 seed 预检；完整 6 fits 不足就报告限制，不能隐藏坏 seed |
| 批量并发 | 初始 2，验证后全体最多 8 worker | 搜索嵌套 worker 同样计数；在线标准延迟单独测 |
| 纠错轮数 | 默认 1 | 第二轮需要 Gate C2 和事先批准的剩余预算，不自动追加 |
| 本地重计算累计运行窗口 | 默认 <=28,800 秒 | 是预算上限，不是交付时间估计；不得据此要求用户等待或开无限后台进程 |

默认 8 个正式 fits 是防止参数爆炸的上限，不保证可以覆盖 E0—E4 全部可选实验。复用有可靠 hash 的历史基线节省配额；至少优先 A/B 和最关键的下一项对照。若要比较价值/新数据但 fit 配额不足，标 DEFERRED，不挪用隐藏运行。

### 4.2 原子预算账本

主 Agent 在 `budget.json` 与 `budget-ledger.jsonl` 中写每项上限、已用量、预留量、剩余量。作业启动前原子预留，结束时按真实使用核销；崩溃、取消、重试同样留记录。并发启动不能各读取同一个“剩余 24 局”而重复占用。账本不一致立即停止新增重任务。

在线 1 秒与全轮计算上限是不同层级：离线教师可使用另一个有界档，但消耗同一总账；线上每请求一秒不能变成每子 worker、每候选或每层一秒。物理资源超限/OOM 优先保护系统，不通过关闭日志或规则检查争抢速度。

### 4.3 不应过早停止，也不能无限推进

**批次 1：** N00—N04 关键修复＋N05，输出 Gate A2；无需为每个测试请求确认。Gate 通过后继续可执行任务。

**批次 2：** N06 教师资格与可信已有数据上的 N08 A/B 基线。教师 HOLD 时阻塞其纠错任务，不阻塞 A/B；数据缺失时继续结构/推理/延迟测试，不伪造正式训练。

**批次 3：** 条件满足的 N07 单轮纠错、N08 数据/价值对照、N09 消融，输出 Gate C2 与架构决策。不要不顾预算把所有条件实验全部铺开。

**批次 4：** N10 预冻结最终测试、可选策略入口和回退，输出 Gate D2；默认 AI 与远端发布仍不自动改动。

停止重计算的条件：关键规则/语义错误；来源或分区不明；教师无资格；数值失败；在线异常不可控；全局配额用尽；连续受控实验没有可证伪新假设。停止时保存结果与最小复现，继续不依赖该瓶颈的代码/文档任务。

## 5. 已核对可用的基础命令与入口规则

以下命令依据参考提交的 `package.json`。在仓库根目录运行，先记录工作区；本次编写未执行这些命令；运行 Agent 先核对当前 package.json，不把存在的测试当作已经通过。[S15]

```sh
git rev-parse HEAD
git branch --show-current
git status --short
node --version
npm --version
npm run lint
npm test
npm run build
```

针对已有测试文件的定向运行示例：

```sh
npm test -- tools/skirmish_action_semantics.test.ts tools/skirmish_candidate_order.test.ts
npm test -- tools/skirmish_dataset_vnext.test.ts tools/skirmish_dagger_vnext.test.ts
npm test -- tools/skirmish_network_features.test.ts tools/skirmish_dual_head_net.test.ts
npm test -- tools/skirmish_search_teacher.test.ts tools/skirmish_search_teacher_prefilter.test.ts tools/skirmish_neural_search.test.ts
```

APK 报告依赖本机 APK/分析资源：

```sh
npm run apk:training-report -- --check
```

缺少被忽略的 APK 资源时登记 `BLOCKED_RESOURCE`，与代码断言失败分开。没有依赖环境时先按锁文件和项目授权策略准备，不自动升级全部依赖。

参考提交虽然包含网络、DAgger、搜索适配函数，但 `package.json` 没有统一的新双头训练、教师资格与 2×2 评估命令。执行 Agent 必须先查本机是否已有等价入口；确实没有再创建 CLI、参数解析、帮助和 smoke 测试并记录变更。Git 提交遵循现有用户/仓库授权，禁止自动 push。**不得直接执行本任务书未核对过的想象参数，也不得把仅导出函数的模块调用成功当成完成训练。**

## 6. 产物组织与报告契约

建议小型可提交报告放在：

```text
docs/training/reports/<runId>/
  audit-summary.md
  task-status.json
  budget.json
  evidence-index.json
  gate-a2-report.md
  teacher-qualification.json
  neural-search-ablation.json
  model-card.md
```

大型快照、模型、逐步日志继续放在：

```text
training_runs/<runId>/
  inputs/
  snapshots/
  datasets/
  checkpoints/
  evaluations/
  logs/
```

小报告保存大产物的相对路径、字节数、SHA256、生成命令与可恢复位置；不要在文档中暴露服务器凭据、令牌或未经授权的地址。外部存储没有授权就只记录本机路径，不自动上传。

### 运行记录模板（接口建议，待实现并验证）

```json
{
  "schemaVersion": 1,
  "runId": "REPLACE_WITH_UNIQUE_RUN_ID",
  "codeCommit": "REPLACE_WITH_ACTUAL_COMMIT",
  "worktreeDirty": null,
  "dirtyDiffHash": null,
  "sourceDependencyHash": null,
  "taskbookVersion": "v3.0",
  "architectureId": null,
  "onlineProfileId": "online-1000ms-v1",
  "onlineDecisionLimitMs": 1000,
  "rulesHash": null,
  "stateEncoderVersion": null,
  "actionEncoderVersion": null,
  "datasetManifestHash": null,
  "splitManifestHash": null,
  "studentModelHash": null,
  "teacherConfigHash": null,
  "teacherQualificationId": null,
  "command": null,
  "exitCode": null,
  "uniqueStates": null,
  "rootFamilies": null,
  "optimizerSteps": null,
  "samplePresentations": null,
  "startedGames": null,
  "completedGames": null,
  "decisionCount": null,
  "deadlineMissCount": null,
  "decisionEndToEndP99Ms": null,
  "decisionEndToEndMaxMs": null,
  "implementationStatus": "NOT_EVIDENCED",
  "testStatus": "NOT_RUN",
  "qualityStatus": "NOT_EVIDENCED",
  "performanceStatus": "NOT_RUN",
  "artifacts": [],
  "limitations": []
}
```

模板中的 null 是未测量，不是默认成功。实际验证器应拒绝需要这些字段的晋级操作；不能把模板未经填充当成执行报告。

### 每个任务完成报告固定包含

目标与假设；实际改动文件；对应提交/工作区差异；运行命令、退出码与日志；输入输出 hash；测试通过/失败/未运行；真实质量/成本结果；新发现的反例；未解决项；下一任务的准入结论。

禁止用“实现完毕，应该有效”“所有任务已完成但测试未跑”“loss 降低，所以可部署”作为报告结束语。

### 6.1 稳定入口与可恢复状态

小型报告中必须额外提供 `latest-run.json` 或等价指针，指向真实 runId；写 `NEXT_ACTION.md`，让后续 Agent 无需重新通读所有巨型日志即可恢复。指针不可替代原始不可变记录。

建议新增但未实现的关键产物：

```text
docs/training/reports/<runId>/
  document-versions.json
  implementation-diff.md
  experiment-registry.json
  budget-ledger.jsonl
  online-budget-contract.md
  architecture-comparison.json
  depth-decision.md
  gate-a2-report.md
  gate-c2-report.md
  gate-d2-report.md
  NEXT_ACTION.md

training_runs/<runId>/
  decisions/decision-telemetry.jsonl
  evaluations/paired-games.jsonl
  evaluations/online-latency.json
  checkpoints/<model-id>/training-checkpoint.*
  models/<model-id>/deployment-model.*
```

路径是组织建议，已有等价实现可复用；不要仅为凑齐目录而创建空的“通过报告”。`training-checkpoint` 与 `deployment-model` 明确分开；标准化折叠后的部署产物不得当成原优化器状态继续训练。

### 6.2 最终结果必须回答的八个问题

1. 当前代码/数据/模型/规则是哪一版，哪些结果是历史，哪些是本轮真实运行？
2. F01—F08 哪些已复现并修复、哪些已在本机提前修好、哪些仍需证据？
3. NET_A 与 NET_B 的真实层数、参数量、样本/更新预算和各 seed 表现如何？
4. 加深是否带来同预算棋力收益，还是只是离线指标提高；深度与容量能否区分？
5. 策略先验和价值头分别改变了什么；候选是否真正被展开？
6. 目标部署平台端到端 1 秒通过了吗，p50/p95/p99/max、超限和回退是多少；整回合累计多久？
7. 教师与纠错是否合格，当前最强“已验证”候选是谁，是否仅限某任务域？
8. 哪些任务完成、HOLD、BLOCKED 或 DEFERRED，剩余预算多少，下一步具体命令/必要条件是什么？

没有证据的字段填 null/NOT_RUN，不填看似合理的数字。对没有成功运行的命令明确记录错误或不可用原因；不得写“理论上通过”。


## 7. 交给执行 Agent 的启动指令

以下内容与配套 `START_AGENT_PROMPT.txt` 一致，可直接复制；该 txt 只是启动提示词，不是第三份任务书。

```text
你现在是 AncientEmpires 项目的执行 Agent。请直接在当前项目工作区阅读文档、核验已有实现、修改代码、运行测试，并在阶段门槛和资源预算内完成后续实验；不要只总结文档或再给一份计划。

一、先完整阅读项目根目录中的两份文件，分页读到末尾：
1. AncinetEmpires_AI_Agent_Taskbook_20260920.md
2. AncinetEmpires_AI_Progress_Review_20260920.md

本次应使用两份文件中的 v3.0 整合版。任务书是执行规范，审查报告是固定提交的证据背景。若存在 AncinetEmpires_AI_Agent_Taskbook_20260919.md，仅在需要时参考历史内容，不从 T00 重做，也不让旧方案覆盖新版要求。先读取仓库有效的 AGENTS.md 等工程约定。

二、已确定的目标与限制：
- 先修复数据、动作表示、搜索和模型加载正确性，再判断是否增加网络深度。
- 在线每个底层动作的端到端决策目标上限为 1,000 ms，包含排队、状态传递、特征提取、网络推理、搜索、返回和最终合法性校验；不是只限制网络前向，也不是把整回合限定为 1 秒。
- 850 ms 停止新增搜索、950 ms 返回当前合法结果是任务书建议的初始余量；预检后可调小并冻结，不得把总上限悄悄放宽。简单局面提前返回，不故意等满一秒。
- 主要深度对照是 NET_A 干路 [256,128] 与 NET_B 干路 [256,256,128]，策略头均为 [64,64]，有条件的价值头为 [64]。这些是待实现/验证的候选，不是现有 checkpoint 的实际参数，也不保证 B 更强。保留旧模型；NET_C、空间网络和第二轮纠错按条件执行。
- 不自动切换默认 AI，不修改游戏规则来提高指标，不删除历史数据，不覆盖用户未提交改动，不租收费云算力、不上传数据、不自动 push 或发布。

三、执行次序：
1. 先做 N00：记录实际分支、HEAD、dirty diff、文档版本和 hash；核验本机训练产物、模型、日志、分区与教师资格；检查与审计提交 cfb07fcc97aff5c2155d3289ddb834ffd1f7522d 的差异。以实际源码和证据为准，已修复的问题标 ALREADY_FIXED 并复用；不能回退工作区去重现旧问题。
2. 完成 N01—N04 的必要正确性工作，并并行准备 N05。重点处理全局动作索引与局部下标混用、动作目标/招募位置碰撞、最终候选保留、真实对手交接、逐候选回应证据、严格模型加载、数值/恢复一致性、可配置网络层数，以及 1 秒预算和合法回退。先写能暴露问题的反例，再最小修复和回归。
3. 输出 Gate A2 的真实结果。通过后、依赖和总预算具备时继续推进，不要把 Gate 报告当作整项任务的终点。
4. 开展 N06 教师分域资格和可信已有数据上的 N08 A/B 基线。教师未合格时，不采集它的大量纠错标签，但仍可做不依赖该教师的容量/性能实验。
5. 条件满足后完成 N07 单轮纠错、N08 数据/价值对照和 N09 消融。A/B 数据、编码、训练预算和非网络辅助一致；固定节点面板与固定 1,000 ms 面板分开。价值头未合格就保持策略先验＋原评价，不强行启用 S11。
6. 按 N10 完成预注册最终评估、可选择策略入口、回退和交接；选择实战证据支持的版本，不预设必须选更深网络。默认 AI 不自动改动。

四、执行方式与停止条件：
- 先查实际 CLI、参数解析与测试入口；缺失才创建并验证，不向旧脚本传想象参数。
- 实验前按任务书第 4 节建立一个全局预算账本；所有 Agent、worker、模型、seed、失败重试、采集和评估共享配额，不能各算一份。保留最终评估预算，不自动穷举参数网格。
- 基础修复不应只停留在文档。能够安全继续的独立任务直接做；真正缺资源、权限或前置条件时，只阻塞受影响部分并说明原因，不编造运行结果。
- 真实测试没跑写 NOT_RUN，证据缺失写 NOT_EVIDENCED，统计精度不足写 HOLD，资源缺失写 BLOCKED。不要用“实现了”代替“通过了”，不要把旧 v4 的成绩当成新网络成绩。
- 在阶段切换或出现重要发现时简短汇报。当前执行窗口或资源预算结束时保存可恢复 checkpoint、预算账本和 NEXT_ACTION.md，明确仍在运行的进程；不遗留无授权的持续后台任务，不声称未完成工作会自动继续。

五、产物与最终汇报：
小型报告放到 docs/training/reports/<runId>/，大数据、模型和逐步日志放到 training_runs/<runId>/；复用已有等价目录。更新 CURRENT_TRAINING_STATUS.md，但保留历史。

最终给出实际改动文件、运行命令与退出码、代码/数据/模型/配置 hash、测试通过/失败/未运行清单、A/B 各 seed 与分域结果、1 秒端到端 p50/p95/p99/max 和超限/回退次数、整回合累计耗时、教师/价值资格、选定或保留的候选、剩余预算及下一项具体操作。

现在先完整读取两份文档、检查工作区，然后开始 N00 和可并行的正确性任务。
```

## 8. 来源索引与方法依据

所有仓库代码引用固定到同一提交，避免 qwen 后续移动导致结论对象不明。以下代码文件链接可通过替换路径定位：

```text
https://github.com/TatsukiKiruma/AncinetEmpires/blob/cfb07fcc97aff5c2155d3289ddb834ffd1f7522d/<PATH>
```

| 编号 | 路径 / 主要定位 | 支持的内容 |
|---|---|---|
| S01 | `AncinetEmpires_AI_Agent_Taskbook_20260919.md` | 原 T00—T14、阶段门槛、预算、安全和验收口径 |
| S02 | `docs/training/CURRENT_TRAINING_STATUS.md`，顶部 2026-09-18 段落 | 历史模型结果与归档进度；不代表新 qwen 实战 |
| S03 | `.gitignore` | `training_runs/`、日志和 APK 分析资源被忽略 |
| S04 | `tools/skirmish_network_features.ts::encodeGameState/writeEntities/entityRows` | 356 维、4×4 分箱、Top-10 实体、排序和状态压缩 |
| S05 | `tools/skirmish_network_features.ts::encodeGameAction` | 招募 geometry 条件、destroy_town 目标缺失、目标数值、重复扫描 |
| S06 | `tools/skirmish_action_semantics.ts::getActionSemantics` | 已有独立 actor/target/sourceCastle/deploy 语义 |
| S07 | `tools/skirmish_dataset_vnext.ts::migrateLegacyFeatureSample/validateDatasetSampleVNext` | 局部索引错配、哈希校验边界、结果和分区契约 |
| S08 | `tools/skirmish_dagger_vnext.ts::buildDaggerVNextSample/outcomeTrack` | 训练分区隔离、行为/建议双轨、结果视角 |
| S09 | `tools/skirmish_search_teacher.ts` | 默认搜索预算、prefilter/pool/beam、交接、回应与终局评价 |
| S10 | `tools/skirmish_dual_head_net.ts::trainStep` | 手写 CPU 训练、硬标签策略与掩码价值梯度 |
| S11 | `tools/skirmish_dual_head_net.ts::applyMomentum/predictDecision/loadDualHeadModelFromJson/foldInputStandardization` | 平均口径、梯度分配、模型加载与标准化折叠 |
| S12 | `tools/skirmish_neural_search.ts` | 策略先验/价值叶评估/纯网络策略，非有限值处理 |
| S13 | 提交 `f6a5c67bb0f88b743c627f7b8b0db1fa9b79f724` | move 落点坐标修复与标准化折叠 |
| S14 | 提交 `cfb07fcc97aff5c2155d3289ddb834ffd1f7522d` | 策略工厂透传 leafEvaluator |
| S15 | `package.json` 与 `tools/` 目录树 | 可用命令、已有模块与测试文件 |
| S16 | `src/game/ai/planning.ts` | 真实引擎、视角、节点计数、内部 Date.now 计时 |
| S17 | `tools/skirmish_bc_train.ts` 的接口/导入 | v5 支持、共享语义导入、候选接口没有逐候选固定索引 |
| S18 | `tools/skirmish_dual_head_net.test.ts` | 24 条合成数据、900 步过拟合测试；不等于真实棋力验收 |

**方法依据，仅支持路线而非本项目收益保证：**

[R1] Ross, Gordon, Bagnell (2011), *A Reduction of Imitation Learning and Structured Prediction to No-Regret Online Learning*, PMLR 15:627–635。借鉴在学习者诱导状态分布上迭代聚合监督的思路；本项目有限、噪声教师不自动满足其全部理论条件。

```text
https://proceedings.mlr.press/v15/ross11a.html
```

[R2] Anthony, Tian, Barber (2017), *Thinking Fast and Slow with Deep Learning and Tree Search*, arXiv:1705.08439。借鉴“搜索改进策略—网络泛化—网络反过来辅助搜索”的分工；具体收益由本项目消融决定。

```text
https://arxiv.org/abs/1705.08439
```

[R3] Farama Foundation, *Deep Dive: Gymnasium Terminated / Truncated Step API*。规则终局与外部时间截断不能混用；本任务书对无可靠结果的监督价值目标使用掩码。若以后采用 TD/自举，需要另行制定截断 bootstrap 规则；不得简单删掉全部截断状态，也不得把材料裁定混同自然胜负。

```text
https://farama.org/Gymnasium-Terminated-Truncated-Step-API
```



[R4] Node.js 官方 `worker_threads` 文档。用于核对 CPU 密集型 JS 的 worker 使用和线程池复用原则；具体 API 以执行机器实际 Node 版本为准，不要求升级到网页显示的版本。

```text
https://nodejs.org/api/worker_threads.html
```

[R5] Node.js 官方 `timers` 文档，Scheduling timers / setTimeout。支持“回调不保证精确触发”这一运行时边界；N03 的余量、watchdog 和请求隔离是本任务书工程设计，须本机验证。

```text
https://nodejs.org/api/timers.html
```

[R6] He, Zhang, Ren, Sun (2015), *Deep Residual Learning for Image Recognition*, arXiv:1512.03385。仅作为条件空间/残差架构的背景；不证明 4 块/64 通道在本项目最优。

```text
https://arxiv.org/abs/1512.03385
```

本轮整合复核了 [R1]—[R6] 的官方/原始资料入口；项目事实仍来自前序固定提交审计及配套报告，不宣称重新检查了最新 qwen。本文所有新架构尺寸、850/950 ms 分配、有限训练/对局预算与晋级门槛均是待预注册、待实测的执行设计。


**最终交付原则：** 交付“正确的输入与搜索＋可信训练证据＋1 秒内最强的已验证策略”，而不是最深的网络。完整结果可以是 NET_A/NET_B/旧冠军/局部策略；没有收益时保留可运行基线与失败证据，同样完成了一次有效实验。
