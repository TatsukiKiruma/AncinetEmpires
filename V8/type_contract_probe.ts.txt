// Intentional type errors: reduced API contract reproduction, not repo build.
interface Encoded {spatialTensor: Float32Array; globalFeatures: Float32Array}
interface Candidate {semantics: Float32Array}
declare const predictor: { predict(encoded: Encoded, candidates: Candidate[]): void };
declare const state: Encoded;
declare const candidates: Candidate[];
predictor.predict(state.spatialTensor, state.globalFeatures, candidates);
interface Decision {topIndex: number}
declare const decision: Decision;
const index = decision.bestActionIndex;
