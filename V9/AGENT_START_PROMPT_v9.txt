请在 Ancient Empires 仓库执行 V9 任务，不要只再写一份计划。

先完整读取：
1. AncinetEmpires_AI_Review_and_Agent_Taskbook_20260922_v9.md
2. agent_tasks_v9.json
3. evidence_manifest.json
4. 仓库已有 V8 任务书、最新 NEXT_ACTION、对应实际源码和工件。

这些文件可能放在仓库根目录或 AncinetEmpires_V9_20260922 子目录，请先定位实际文件，不要假定本机绝对路径。

审查基准是 path-b-spatial-ai 的 e95b65366186dc55482d5db77346936aaf255ae6。
先核对当前 HEAD、分支、dirty diff、工作树、已有任务进度和模型文件。如果 HEAD 已更新，以实际源码为准并记录差异；不要强制回退、覆盖别的 agent 的修改或重新实施已合并的修复。

第一批必须执行 V9-00、V9-01，随后并行推进 V9-02、V9-03。优先解决：
- 已提交前端仍走 spatial_resnet_v1 和内置旧权重，新训练 v2 并未自动接入；
- v1 策略只消费前24维动作语义，同城堡/同部署格的不同兵种招募存在输入碰撞；
- 前端加载失败仍可能使用初始化权重、无效索引回退legal[0]；
- 最新重评驱动固定读取 v7_01/checkpoints，基础模型是100状态冒烟权重，而非较大批次v2；
- 主线程 queueMicrotask + setTimeout 不能真正抢占同步搜索，取消/过期动作还需应用端保护。

先建立实际可校验的 model_registry：路径、SHA-256、编码版本、训练目标、数据/分区/实际消费清单、训练来源、可获得性。前端、评测、DAgger使用共享策略契约，并给出同fixture同权重的动作一致性证据。保留Heuristic默认，只新增明确标记的实验v2入口，不自动上线。

本地若存在较大基础v2，请核对报告中的SHA：
1721ca177ec729457b7dde0676573c3312bd0b96331ea9b29ddbb902b635af92
该值是待本地复核的历史报告值，不是可以直接写入并跳过校验的常量。
若文件缺失，明确ARTIFACT_MISSING；不得改用随机/旧模型仍登记为该SHA。继续做不依赖缺失工件的源码与fixture测试，并为新训练创建新runId。

冻结权重重评先跑连通性，再跑同协议开发矩阵：Heuristic / 正确基础v2 / S00 / S10。固定节点和真实墙钟实验分开；禁用或置零先验须明确候选与并列排序规则。保存真实轨迹、W/L/D/T、错误、模型身份、延迟和搜索覆盖；20场/臂仅用于开发诊断。

随后执行V9-04～V9-07，真实修数据、分区并训练：
- 修地图与金币共模、400步前缀偏置、完整根对局记录；
- 修commanderReserveLevel/Exp哈希遗漏，根ID不取模；
- 同根及派生轨迹不得跨train/val/holdout，补齐验证地图/座位覆盖；
- 区分生成行数、唯一状态、根家族、实际消费和重复曝光；
- 复用当前严格64状态过拟合测试，然后先32ch/2块完成实际10k训练状态学习曲线，再依据结果进入30k；
- 使用搜索教师之前先验证其质量；普通Heuristic蒸馏在数据/契约门槛通过后不必无限等待所有搜索优化；
- DAgger必须比较“旧数据继续训练”与“加入新纠错”，B单步改进和C接管改进分开；
- 扩深与value学习仅在明确前置条件满足后进行，不预设它们必然提升。

isolated_contract_probes_v9.cjs 是源码公式/输入投影的独立反例，不是仓库集成测试。可以运行理解问题，但必须另写直接调用真实仓库模块的回归测试。拿不到真实pending fixture、checkpoint或数据时，不得直接return让测试显示通过。

保护所有历史报告、权重和用户修改；每批使用新的runId。不要自动push、租算力、修改游戏规则或生产默认。1秒目标按一次原子请求端到端测量，包含fallback准备、编码、推理、搜索和返回；不得裁剪超时数字。取消/过期返回不得落子。

每批结束交付：实际源码diff、真实运行命令/退出码、测试范围、数据实际计数、参评模型SHA、对局矩阵与原始轨迹、延迟、明确未运行项。更新agent_tasks_v9.json状态，不把IMPLEMENTED/TESTED/MEASURED混成ACCEPTED。

立即开始第一批的只读身份核对，然后实施和测试可执行的修复；不要停留在复述任务书。
