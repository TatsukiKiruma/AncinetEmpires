# AncientEmpires：AI 训练进度审查与升级决策说明

**版本：2026-09-20 / v3.0 整合版**  
**必读配套任务书：`AncinetEmpires_AI_Agent_Taskbook_20260920.md`（v3.0）**  
**审计对象：`TatsukiKiruma/AncinetEmpires` 的 qwen 分支固定提交 `cfb07fcc97aff5c2155d3289ddb834ffd1f7522d`**  
**该提交时间：2026-09-19 20:53:37 UTC / 2026-09-20 05:53:37 Asia/Tokyo**  
**文档性质：历史证据背景＋新增决策约束，不是本轮训练结果。**

> 本报告解释为什么做这些任务；配套任务书负责规定如何执行。两份放在项目根目录即可启动。2026-09-19 原任务书可保留作历史，但不是必须提供的第三份文件。

## 0. 阅读约定、证据范围和新要求

本轮编写实际读取了前版任务书与进度审查文件，并将用户随后提出的网络深度与 1 秒决策要求整合进去；没有在本轮重新审计移动中的远端分支，没有访问用户训练机器，没有运行项目测试、训练或对局，也没有修改 GitHub 仓库。

因此，本报告中“当前实现”均指上面固定提交的前序审计快照，不保证执行 Agent 打开项目时仍完全相同。源码已变、问题已修复或本机已有实验时，以实际可追溯证据更新，不能为符合报告而把工作区回退。源码发现的条件分支与结构性问题，不等于已经测得其影响比例或棋力损失。

两份文档有意区分三类内容：**已核查源码/历史报告事实**、**基于事实的技术分析**、**尚待执行的实验建议**。本轮新增“NET_A/NET_B 对照、850/950 ms 余量、资源上限”等均属第三类，不是现有运行成绩。方法资料 [R1]—[R6] 已列入任务书并在本轮复核原始/官方入口；源码索引 [S01]—[S18] 对应固定提交。

用户已明确允许决策时间放宽到 **1 秒**。本轮工作约定为“每个底层动作、调用方端到端 1,000 ms”，不是整回合一秒，也不是只测网络前向。这个上限允许适度更大的网络和更多有效搜索，但不代表每次必须消耗一秒，也不构成昂贵/无限训练授权。

## 1. 总体判断

项目已经从旧线性行为克隆与启发式辅助，推进到规划接口、有限预算搜索教师、纠错数据契约、紧凑策略/价值网络和网络辅助搜索的工程原型阶段。主要模块有实质性建设，不需要推倒重来。[S04]—[S12][S16][S17]

但是，**工程进度快于能够核验的棋力证据**。在前序审计取得的已提交状态报告中，最近可引用的实战成绩仍属于 2026-09-18 的旧 v4 与辅助组合。审计没有取得新教师、DAgger 新轮次、双头网络或网络辅助搜索的完整训练/配对评估产物。因此不能认定新路线的质量 Gate 已通过，也不能断言用户本机没有运行。[S02][S03]

推荐执行路线仍然是：

```text
核验已有成果
  → 修复数据、动作表示、搜索交接与严格加载
  → 可信行为数据上的 NET_A/NET_B 对照
  → 合格教师在学生自访状态上的有限纠错
  → 策略引导搜索，价值头单独验收
  → 1 秒端到端与独立质量评估
  → 可选择、可回退的已验证候选
```

教师资格和已有可信数据上的深度实验可以分别推进。教师不合格时不强制采集其标签；已有数据不满足新表示契约时，也不能强行训练。出现局部阻塞时继续独立的代码、反例、推理与性能任务。

## 2. 进度要分三条线看

| 维度 | 固定提交/历史报告支持的状态 | 不能由此声称 |
|---|---|---|
| 历史训练 | 状态文档记录 2,500 局、977,737 条历史样本归档并完成当时校验 | 新双头网络已经用同样规模完成训练 |
| 新工程 | 共享语义、vNext、规划、MLP、搜索、DAgger 映射、双头网络及接入代码存在 | 所有阶段测试和质量验收都已经通过 |
| 新棋力 | 前序审计缺少新模型/新搜索的完整配对评估证据 | 已完成专家迭代、已超过旧组合或已满足 1 秒 |

以上依据分别为 [S02]、[S04]—[S12][S15]—[S18]。仓库 `.gitignore` 排除了 `training_runs/`、日志和部分 APK 分析资源，必须先在本机核验；不要强行把大型数据提交 Git，也不要把缺失资源与测试断言失败混为一谈。[S03]

最新被核查的提交仅证明策略工厂透传了 `leafEvaluator`，使网络价值能通过工厂接入。邻近改动修复了 move 落点坐标的一类歧义。这些是实现变化，不是训练成绩单，也未覆盖所有动作目标语义。[S05][S13][S14]

源码中的微型过拟合测试使用 24 条合成样本、900 个训练步；其意义是验证训练机制。它不能代替真实游戏微型可学性，更不能代替自然对战。[S18]

## 3. 历史成绩与其局限

下面是 `docs/training/CURRENT_TRAINING_STATUS.md` 在 2026-09-18 的同一历史独立种子矩阵；本轮没有重跑。[S02]

| 策略 | 正常开局自然胜 | 优势残局自然胜 | 整个矩阵超时 |
|---|---:|---:|---:|
| 旧 v4＋启发式收官辅助 | 8/16（50.0%） | 11/12（91.7%） | 1/28 |
| 旧纯 v4 | 2/16（12.5%） | 8/12（66.7%） | 4/28 |

**能作出的分析：** 该矩阵里组合策略的观测结果更好，但缺少“无 BC＋完全相同辅助”，无法精确分出模型自身增益。后续必须加入这个对照，不能只比较纯模型与模型加一整套改进。

正常开局与复杂路径仍值得重点关注：同份报告有 Icy Paths 正常开局 0/4 的历史记录。开发时已经看过/调过的这些局面应标开发弱项，不能继续声称是封存测试。[S02]

同份报告还记录某个按验证指标选出的 25% 混合补训权重，在实际对战中退步并被放弃。因此，离线准确率上升不能代替晋级；这不是本报告对所有补训方案的否定，只是已有反例。[S02]

文档更下方的 698/2,500、27.9% 属更早快照，不能覆盖顶部后来的归档进度。同样，历史 977,737 条样本不能自动变成完整状态新编码器的可用数据。[S02][S07]

## 4. 需要先解决的八类问题

以下结构与控制流来自固定提交审计，实际受影响样本、速度和胜率必须由 Agent 实测。已经修复的项应登记 ALREADY_FIXED，不重复重写。

| 编号 | 发现及含义 | 执行定位 |
|---|---|---|
| F01 | 招募几何编码依赖 actor；招募无 actor。destroy_town 未读显式 target；攻击/治疗/支援目标缺精确位置，可能把重要动作编码相同 | N02；`skirmish_network_features.ts::encodeGameAction` |
| F02 | 重标注迁移用 candidates.findIndex 写 fixedActionIndex，混用局部候选位置和全局固定索引 | N01；`skirmish_dataset_vnext.ts` |
| F03 | 多来源 pool 最后统一按启发式排序截 beam，网络/关键动作来源标签不提供最终保留保证 | N03；`skirmish_search_teacher.ts` |
| F04 | 未真正切到敌方时可能进入对手回应；全局 sawOpponentPhase 不能证明选中候选有回应证据 | N03；同模块交接与回应 |
| F05 | 4×4 分箱有损，不等于精确 padding；Top-10 实体、状态压缩和 ID 兜底排序需审计 | N02；状态编码/实体排序 |
| F06 | 价值梯度与报告分母不同；缺权重可能留下随机初始化；标准化/非有限输出与推理梯度分配需严格处理 | N04；双头网络与接入 |
| F07 | 有 sampleId hash 不证明快照存在/内容一致/结果已验证；playerId 与 allianceId 语义混用 | N01；vNext/DAgger 结果契约 |
| F08 | 候选重复扫描、软上限、未真正限流和双时钟；“轻量”不能只看名称，内部 timeout 不等于端到端控制 | N02/N03/N05；编码与规划预算 |

依据：[S04]—[S12][S16][S17]。完整反例、修复步骤、验收与停止条件见配套任务书相应 Nxx，而不是由本表直接判定所有历史训练无效。

### 4.1 为何动作歧义优先于模型加深

同一状态中，若两个动作具有完全相同编码，当前共享状态嵌入的确定性评分网络必然给相同 logit。更深/更宽的网络不能恢复输入没有包含的区别。这是函数输入相同导致输出相同的逻辑结果。

正确方案是复用已有 `getActionSemantics()`，统一行动者、真实目标、来源城堡、部署落点；新表示独立版本。不要向特征塞任意 actionCode/ID 哈希来把碰撞数字降到零，因为那不等于学到了目标语义。[S06]

### 4.2 为何“接入了网络”还需要候选存活审计

网络推荐可能在预筛或最终 beam 被删掉；仅有网络调用次数不能证明其参与了决策。后续要记录各来源进入最终集合的比例、实际展开数、选中动作是否改变及对应回应覆盖。否则不能把“网络不起作用”直接归因于模型能力。[S09][S12]

### 4.3 为何默认一步回应不够说明搜索充分

参考默认 `opponentPhaseActions=1`，只看一个对手底层动作，未必看到移动后攻击/占领。要给指定战术组合做有界覆盖，按候选记录真实敌方行动；不必立即做完整回合树，也不能把动作层数自动叫作回合数。[S09][S16]

## 5. 网络深度：新的执行决定

### 5.1 当前网络的准确描述

参考实现采用两层共享干路、两层策略隐藏头和一层价值隐藏头。因此从状态到策略输出的路径已有 4 个隐藏层；不能把整个网络统称为“两层”。网络是纯 TypeScript 的紧凑模型原型；`qwen` 是分支名，不表示在微调 Qwen 语言模型。[S10][S11]

共享干路固定层数，spec 与张量命名也固定。增加第三层必须同时改前向、反向、参数校验、保存/恢复、加载和推理，而不是给不存在的 `--depth` 参数赋值。[S10][S11]

### 5.2 本轮优先 A/B，不直接上很深网络

| ID | 共享干路隐藏层 | 策略隐藏头 | 有条件价值隐藏头 | 状态 |
|---|---|---|---|---|
| NET_A | 256 → 128 | 64 → 64 | 64 | 必要的修复后基线 |
| NET_B | 256 → 256 → 128 | 64 → 64 | 64 | 主要适度加深候选 |
| NET_C | 256 → 256 → 256 → 128 | 64 → 64 | 64 | 条件扩展 |

这是建议配置，不是现有模型实际宽度或实测最优值。输入维度由 N02 修复后的契约决定。NET_A/B 保持同一编码器、候选头、优化器与数据预算，先 policy-only 对照，再有条件增加价值监督。

增加一层同时增加参数量；A/B 可回答哪种架构更适合本预算，不能自动证明纯深度因果。确需分辨，另做参数量近似匹配的两层加宽，不在第一轮无条件加满所有配置。

首选给共享干路增加适量容量，是因为同一个状态嵌入可服务多个候选。实际耗时仍由编码、干路、K 个候选头与搜索共同决定；如果更深模型让有效推演减少，1 秒内棋力可能反而下降。必须测完整系统，不能凭模型参数量判断。

### 5.3 空间/残差网络的条件

若表示审计表明关键地形连通性、单位关系或实体截断信息缺失成为瓶颈，再设计精确网格＋实体编码。4 残差块/64 通道可作一个小起点，但不是本轮默认重构任务。残差论文仅提供架构背景，不能证明该配置在本项目最优。[R6]

训练未收敛时，先分清实现、表示、标签、优化与容量；3 个 epoch 是预检起点，不能单凭其结果判定大模型无用，也不能以“再训可能有效”为由无上限训练。

## 6. 1 秒预算：新的在线验收约定

### 6.1 边界和时间配置

```text
t0：调用方接受一个底层动作的决策请求
    排队/状态传递 → 编码 → 推理 → 搜索 → 返回/合法校验
t1：调用方已拿到可提交给引擎的合法动作
要求：t1 - t0 <= 1,000 ms
```

推荐初始软截止为累计 850 ms 停止新增搜索、950 ms 返回当前合法答案，给 1,000 ms 目标留余量。两个数字不是额外时长，可在预检后调小并冻结；不能每个候选/子树/worker 各重新获得一秒。

模型在接受请求前加载可以单独记录启动时间；如果在第一请求里懒加载，就计入第一请求。排队、通信和异常回退不能排除。每动作一秒可能累计成较慢回合，因此另报每玩家回合和整局的 AI 决策时间；不要求简单动作故意等满一秒。

参考搜索默认是 3,000 ms、984 节点、预筛宽度 Infinity；这不是新的在线要求。离线教师可使用独立有界 profile，但必须登记离线成本，不能直接冒充在线 1 秒策略。[S09]

### 6.2 运行机制和边界

建议先产生合法兜底，再在预算内持续改进；主/UI 线程不做不可中断的大搜索。Node 的 CPU 密集型工作可用常驻 worker，浏览器按实际支持适配 Web Worker；队列、过期请求和错误回退都要测试。官方 Node 文档支持线程池复用，且明确 timer 不保证精确时刻触发。[R4][R5]

因此，本书要求可测试的截止逻辑和真实平台测量，不作“任何负载下绝对永不超时”的虚假保证。有限验收样本中若存在超 1,000 ms，在线 Gate 暂不通过并保留记录；不能只报 p95，也不能删冷启动/慢局面。

### 6.3 搜索优先投入

增加预算应优先让关键候选真正被展开、让对手身份正确且覆盖移动后攻击/占领、让选中候选拥有可比较的回应证据。再考虑扩大 beam 和更深网络。搜索节点数不等于有效战术深度，更不等于胜率。

## 7. 下一阶段实验应该回答什么

**深度主问题：** 在同一套修复后的可信数据上，NET_B 是否比 NET_A 更好？先看纯策略和同样辅助，再看同搜索配置下的固定节点和固定 1,000 ms 两个面板。实际参数数、训练呈现次数、各 seed 和失败运行全部披露。

**数据问题：** 在一种冻结架构上，新纠错数据是否有净收益？教师须先取得明确任务域资格；教师与学生动作比较要有匹配预算和真实回应，不把未执行建议标成已经获胜。

**价值问题：** 对一个合格联合网络，分别开关策略先验与价值叶评价：

| 搜索臂 | 网络先验 | 网络价值 |
|---|---|---|
| S00 | 关闭 | 关闭 |
| S10 | 开启 | 关闭 |
| S01 | 关闭 | 开启 |
| S11 | 开启 | 开启 |

四臂是同一网络的推理用途开关，不是四次不同训练。价值不合格就不运行 S01/S11；训练 policy-only 与联合网络的区别单独对照。若 S10 最强就交付 S10，不要求模块必须全开。

**泛化与时延问题：** 所有派生快照、角色变换、续局继承根家族分区；同地图新 seed 不是未见地图泛化。主指标自然胜/启动对局，超时/停滞/错误独立列出；置信区间按根家族聚类。24 场可筛明显退步，但不能包装成精确的五个百分点提升结论。最终留出只在冻结候选后使用，不能反复调参。

DAgger、专家迭代和终局/截断分离的原始依据见 [R1]—[R3]。它们提供方法原则，不保证有限且有噪声的教师在本项目必然有效。

## 8. 任务映射与可接受的终点

| 任务 | 重点 |
|---|---|
| N00 | 核验工作区、已有训练证据、两份文件版本与总预算 |
| N01 | 固定索引、快照完整性、分区、玩家/联盟视角、真实结果 |
| N02 | 统一动作语义、可区分表示、新版本与编码成本 |
| N03 | 最终候选、真实对手阶段、1 秒端到端、worker 与合法回退 |
| N04 | 数值/梯度/恢复、严格加载、可配置网络深度与推理一致性 |
| N05 | 深度和搜索对照、固定节点/1 秒面板、预注册与晋级 |
| N06 | 教师分任务域资格，不合格则不扩大标签生产 |
| N07 | 一轮有限学生自访状态纠错与可靠轨迹 |
| N08 | NET_A/NET_B、有限数据效果、有条件价值、可选深度扩展 |
| N09 | 同网络 2×2 推理用途消融与完整系统质量/成本 |
| N10 | 冻结测试、可选择候选、回退、状态和交接 |

Gate A2 通过后不必停在计划层，应在实际权限和预算内继续。教师阻塞、资源缺失或证据不足只影响对应任务；不能编造全流程完成。具体默认全轮对局/训练/worker 上限和阶段进入条件以任务书第 4 节为准，报告不再定义第二套预算。

最终结论可以是：B 更适合 1 秒预算；A 更可靠；只有策略先验有用；只有残局教师合格；或者新候选没有可信收益。**这些都比“代码写到了某一任务编号，所以完成”更有意义。**

## 9. 源码索引与核验路径

固定提交文件统一位于下列前缀；这里给出可直接定位的路径和函数，完整编号与研究/官方资料位于配套任务书第 8 节。

```text
https://github.com/TatsukiKiruma/AncinetEmpires/blob/cfb07fcc97aff5c2155d3289ddb834ffd1f7522d/
```

| 编号 | 路径/函数 |
|---|---|
| S01 | `AncinetEmpires_AI_Agent_Taskbook_20260919.md` |
| S02 | `docs/training/CURRENT_TRAINING_STATUS.md` 顶部 2026-09-18 记录 |
| S03 | `.gitignore` |
| S04/S05 | `tools/skirmish_network_features.ts` 状态与动作编码 |
| S06 | `tools/skirmish_action_semantics.ts::getActionSemantics` |
| S07 | `tools/skirmish_dataset_vnext.ts` 迁移与校验 |
| S08 | `tools/skirmish_dagger_vnext.ts` 行为/教师与结果轨道 |
| S09 | `tools/skirmish_search_teacher.ts` 默认参数、预筛/pool/beam、交接/回应 |
| S10/S11 | `tools/skirmish_dual_head_net.ts` 结构、训练、加载、推理、标准化 |
| S12 | `tools/skirmish_neural_search.ts` |
| S13/S14 | 提交 `f6a5c67bb0f88b743c627f7b8b0db1fa9b79f724` / `cfb07fcc97aff5c2155d3289ddb834ffd1f7522d` |
| S15 | `package.json` 与 `tools/` 目录 |
| S16 | `src/game/ai/planning.ts` |
| S17 | `tools/skirmish_bc_train.ts` 候选接口、v5 支持及共享语义 |
| S18 | `tools/skirmish_dual_head_net.test.ts` 合成测试 |

Agent 更新状态时必须明确 actual HEAD 与本审计快照不同之处，并附本机测试/模型/配置 hash。不要把本报告本身作为已经修复、已经训练或已经通过 1 秒验收的证据。
