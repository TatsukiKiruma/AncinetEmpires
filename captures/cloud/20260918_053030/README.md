# 北美服已结束对局下载结果

本次从当前账号可见的“已结束的游戏”列表获取 145 局。共请求 9 页，其中末页为空；下载失败 0 局，重复对局 ID 0 个。

- 原始响应：`game_get_*.bin`
- 导出的明文动作流：`game_get_*.replay.plain.act`
- 对局 ID、文件名、哈希及下载状态：`download_report.json`
- 动作流解析明细：`replay_report.json`
- 汇总与完整性核对：`summary.json`

已解析 145 局，共 117333 条动作；全部标记为 CLOSED。文件大小和 SHA-256 核对通过，外层响应无残留字节。与历史回放响应字节完全相同的文件为 0 个；此口径不等同于按历史对局 ID 或动作内容判重。

经用户授权，本次仅对北美服务放宽 TLS 证书校验；不跟随重定向。登录令牌未写入本目录。

列表可见范围不等于服务器所有私有或已删除对局。下载期间列表可能更新，本次结果不是服务端原子快照。尚未执行项目引擎的 strict/forced 重放验证，不将结构解析成功视为训练质量验证通过。
