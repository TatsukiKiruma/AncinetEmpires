请在当前AncinetEmpires项目中执行指挥官重招募专项及后续AI改进，不要仅再写一份计划。

先读取实际AGENTS.md、当前分支/HEAD/dirty状态，保留用户改动。完整阅读根目录：
1. AncinetEmpires_AI_Commander_Review_20260921_v6.md
2. AncinetEmpires_AI_Agent_Taskbook_20260921_v6.md
3. AncinetEmpires_AI_Agent_Tasks_20260921_v6.json（仅导航）
旧v5/v4文档保留；v6调整顺序，v5仍提供详细技术规范。审查基点为空间分支a0a9dcdd483cb951a72734805504c947176d5d79，qwen对照85a87a0edb64de18a8b8eced9226ffb1cd1b48e9；若代码更新，先验证，已修者ALREADY_FIXED。

用户优先目标：解释指挥官阵亡后金币足够却不重招募；落实正确SD机制进入数据、模型和评测；对全部当前有效学习模型重训，并分别验证每个策略；随后继续此前修复和数据/深度实验。

依次执行C60—C66：冻结所有旧模型/策略并捕获失败状态；跑真实死亡→计数→涨价→合法招募→扣费→pending处理→再次死亡重招生命周期；记录规则层/候选层/模型层/执行层的决策漏斗；最小修复真实入口的SD规则、空间兵种与动态价格编码、policy全局条件、状态重建、候选、根分区、不可变checkpoint和加载；扫描2500局等实际资产的重招募覆盖、补合格专项课程；对活跃BC类、NET_A、NET_B、Spatial及发现的其他有效学习版本逐一重训；旧/新模型和全部wrapper在同一SD协议下换边对比。

重点：规则本来就有重招募及涨价。不要凭“没招”就重写核心规则。无参createDemoState默认禁指挥官购买，先核查实际入口；SD常规费400+100*死亡次数，首次初始指挥官阵亡后的重招通常500；price=null不是免费。SO/战役规则不统一改成SD。当前指挥官人口为0，仍受单位数量和城堡/pending约束。不能把缺指挥官或一次阵亡当自然失败，也不能机械地有钱必招。

Heuristic/Random/S00无学习参数则不伪造训练；NET_B的1ply和S10共享权重只训一次但分别测；BC/blend/收官同理。清单上缺模型/数据要逐项BLOCKED_RESOURCE，不用fallback冒充已测试模型，不把只训Spatial写成全部完成。value数据不足先policy-only，不能伪造自然标签。

C66专项比较后继续C67剩余v5修复，特别是真正Worker与Auto/Sandbox接线、空间策略搜索、有效对手回应、统一评测和加载。独立浏览器工作可并行，不阻塞可信离线重训。然后按C68做同输入下3万/15万×32ch2块/4块对照；有收益才30万/合格训练分区全量。C69条件性一轮学生纠错和value；改变输入/标签后对所有受影响模型重训，C70重新跑其全部策略并冻结最终留出。

训练前先通过编码/重建/候选/标签/分区的关键反例，但不要等所有UI工程完成才训练。不得直接运行会覆盖src的旧训练脚本。所有数据、checkpoint、有效配置、规则、编码和评估hash绑定；真实记录生成报告，空集/全截断/缺hash/泄漏/错价格不得PASS。未知指标为null，状态分实现/验证/棋力/运行时四列。

禁止删除历史数据模型、覆盖dirty、自动切分支、改默认AI、push/发布、读无关凭据或调用收费云。按新任务书先冻结本轮本地预算；保留历史累计和分类更正，不让旧助手建议30k/2000查询成为永久锁，也不绕过用户明确硬限。每底层Action端到端1000ms目标，真实超限如实记；非硬实时浏览器不作无限保证。

完成实际代码、测试、训练和对比。资源不足保存已完成结果与具体缺口并继续独立任务，不编造日志/胜率、不无限重试。每阶段更新task-status、all-model-training-coverage、all-policy-before-after和NEXT_ACTION。最后给出每个模型/策略真实状态、具体命令、退出码、产物hash、失败案例及是否保留原冠军。
