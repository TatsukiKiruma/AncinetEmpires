# v6 文档包

将本目录中的任务书、复核报告、任务JSON、测试目录JSON和启动TXT放到项目根目录，保留旧v5/v4文件。复制启动TXT给执行Agent。

主文件：AncinetEmpires_AI_Agent_Taskbook_20260921_v6.md。
复核报告解释已确认的规则/入口/编码问题；任务书安排实际修复、全模型重训与全策略比较。
两个JSON分别是任务导航与30个未执行测试规格，不是训练或验证结果。

本次仅进行源码/文档核查和文件编写，没有执行用户项目训练、完整对局或浏览器测试；没有修改远端仓库或默认AI。

SHA256SUMS.json可用于校验本包文件；自身不列入其校验清单。文档里的资源数字是新的本地规划建议，用户明确硬限制优先。
